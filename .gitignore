.venv/
__pycache__/
*.pyc
.DS_Store

# Local / generated
.env
.env.*
*.log
*.wav
*.mp3
build/
Desktop/

# Dictation dumps and machine-local state (never in this tree, but belt-and-suspenders)
LocalFlow-audio/
*.app/

# V2 private evidence (belt-and-suspenders; the evidence root itself lives
# outside this repository under ~/Library/Application Support/LocalFlow/v2-evidence/)
docs/v2/private/
*.db
*.sqlite
LocalFlow.log
LocalFlow.log.*
v2-evidence/
*.flac
*.m4a
# M01 remediation (M01-AUDIT-14): SQLite companions, renamed backups and
# unpublished snapshot runs. Content signatures are enforced by
# tests/v2/test_baseline_manifest.py regardless of filename.
*.sqlite3
*.db-wal
*.db-shm
*.db-journal
*.db.bak
*.partial/
# Note for M02: when synthetic fixture databases are wanted under
# tests/v2/fixtures/, add a `!tests/v2/fixtures/**` negation here — the
# global *.db rule above will otherwise exclude them silently.

# Audit input documents: local review material, kept out of the repository
docs/LocalFlow_M07_Read_Only_Audit_d3d3e4b.md
