# M02 Handoff — Dated observability, persistent jobs and lossless import

**Milestone:** M02 · **Requirements:** LF-R02, LF-R03, LF-R20, LF-R25, LF-R28, LF-R30 · **State:** `implemented_pending_human`
**Base commit:** `4bd0187ae2445a472684c9c902c9548f58973ac6` (branch `main`, remote `scalinity/LocalFlow`)
**Tree at start:** clean (M01 committed at `4bd0187`; tree verified clean before changes). No other worktrees; no other session owned any file M02 touched.
**Tree at end:** M02 adds `localflow/v2/` (new package), `tests/v2/logging/`, `tests/v2/storage/`, `tests/v2/training/`, `scripts/v2/{import_legacy,view_events,benchmark_m02}.py`, `docs/v2/{contracts/store.md,acceptance/M02/,benchmarks/20260921-230202-m02/}`; modifies `localflow/{app,cleanup,audio,config}.py`, `scripts/v2/parse_legacy_log.py`, `tests/test_stuck_overlay.py`, `docs/v2/{STATUS.json,contracts/INDEX.md}`. `localflow/{stt,inject,hotkey,overlay,permissions}.py`, `scripts/build_app.sh` and all M01 artifacts are untouched.

## What shipped

1. **Dated event logging (Spec S07, EV-02).** `localflow/v2/eventlog.py`: JSONL envelope schema version 2 under `~/Library/Logs/LocalFlow/events-YYYY-MM-DD.jsonl` (0700 dir / 0600 files), size roll at 10 MiB + UTC-day boundary with age-monotonic suffixes, 14-day/100 MiB retention that keeps files mentioning unresolved jobs and never manages a second writer's pid-prefixed files, bounded priority queues (CRITICAL retried with backoff or flagged degraded, LOW coalesced/dropped with counters), disk-full degradation that never crashes or silently drops terminal events, and a TTY mirror that replaces the old prints. `scripts/v2/view_events.py` renders the human view (UTC/local display) and writes the content-free redacted export. Third-party stderr (tqdm, download progress) is captured into bounded, content-free diagnostic records around model loads.
2. **Persistent store (Spec S08, EV-03).** `localflow/v2/store.py`: single-writer SQLite `~/Library/Application Support/LocalFlow/v2.db` (store schema v2 = v1 tables + hot-path indexes; `training_schema_version` 1), per-op transactions with backup-before-migration, loud refusal on malformed core tables, idempotent repair for lost tables. Jobs follow the contract state machine (idempotent, stale-discard, exactly-one terminal outcome); artifacts are immutable with sha256 and exact metadata; audio persists as lossless IEEE-float32 WAV (S29.5). The legacy `dictations` compatibility view sits over verbatim `legacy_dictations` rows.
3. **Lossless import.** `localflow/v2/importer.py` + `scripts/v2/import_legacy.py`: stats.db imported from a verified SQLite backup (never the live file), dictionary/transforms as legacy-revision artifacts, log pairs as unknown-date artifacts with identity `legacy:<sha>:<line-range>`; atomic artifact+bookkeeping ops make crashes converge on re-import; appended logs reconcile by verified byte prefix. **Real run:** 7 rows (280/277/350.3/11, instants 2026-07-04T21:15:42.804Z–21:38:40.276Z preserved), 7 terms, 2 transforms, 754 log pairs; re-run adds zero rows; sources byte-identical; consistency ok.
4. **Training evidence (Spec S29.1–S29.4, EV-19 initial).** `localflow/v2/training.py`: opt-in consent revisions (absent = disabled — nothing is collected until the user chooses it), live collector around the existing ASR/cleanup stages (audio attached before model execution; exact rendered prompts retained byte-for-byte; raw/proposal/applied artifacts with parent links; append-only revisions; family IDs; `posted_unverified` outcome per the targets contract), honest missing reasons for every later-milestone field family, secret quarantine (content-free events), leases (30-day training buffer, pinned survives, 7-day history can't override), 30-day unreviewed-buffer expiry, delete-everywhere with content-free tombstones, orphan sweep with grace. Minimal menu surface: Collect Training Evidence / Pause Collection / Exclude Last Dictation / Mark Last Dictation Correct.
5. **Pipeline wiring.** `app.py` prints → events; jobs minted at hotkey-down per the contract; store/collector calls guarded so a store stall or evidence-write failure can never drop a dictation; retention runs off-main daily. `cleanup.py` gained optional notifier/observer hooks (behavior verified unchanged: 51/51 model-backed cases). `config.py` gained the S25 retention knobs (visible, adjustable).

## Contracts and decisions

- New `contracts/store.md` (owner M02): writer discipline, frozen identities, legacy class never pruned, lease/deletion precedence, events↔store unresolved-job coupling. `contracts/INDEX.md` updated. No frozen identifier was repurposed (`job_id`/`family_id`/`example_id`/`revision_id`/`consent_revision_id`, `legacy:<sha>:<lines>`).
- Migration v2 (indexes) was applied to the live store with a pre-migration backup under `v2-evidence/backups/`.
- Job states in the live pipeline: `capturing → queued → transcribing → cleaning → ready_to_insert → insertion_posted → insertion_unverified`; empty output → `saved_not_inserted`; worker exception → `failed_recoverable`; user/min-duration cancel → `cancelled`. V1's unobservable paste is recorded as `posted_unverified` (contracts/targets.md baseline note).
- Registry unchanged (33/22 hold; M02 adds no S05/E08 rows). No ADRs — no spec deviation was accepted.

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M02/results.json`. All exit 0, cwd = repo root:

- Pre-change baselines (before any edit): 4× tests/v2 suites, `tests/test_stuck_overlay.py` 5/5, `tests/test_cleanup.py` 51/51 (model-backed, avg 0.90 s).
- Post-implementation: `tests/v2/logging/test_event_writer.py` 13/13 (automated, EV-02); `tests/v2/storage/test_store.py` 17/17 (EV-03); `tests/v2/storage/test_import.py` 7/7 (EV-03); `tests/v2/training/test_collector.py` 11/11 (EV-19); `tests/v2/training/test_live_pipeline.py` 2/2 (model-backed native Mac, offline); `tests/test_stuck_overlay.py` 5/5; `tests/test_cleanup.py` 51/51 (avg 0.88 s); `tests/v2/test_legacy_parser.py` 5/5 (refactor byte-compatibility); registry/manifest/snapshot suites green.
- Real-data: `scripts/v2/snapshot_user_data.py` (all sources unchanged) → `scripts/v2/import_legacy.py` (lossless, idempotent, consistency ok; live store now schema v2).
- Benchmarks (`docs/v2/benchmarks/20260921-230202-m02/m02.json`, re-run on post-review code): emit p95 0.057 ms; audio callback p95 0.004 ms (pure in-memory, no db/logging work); 50k-row inserts at ~3.9k rows/s with 0.02 ms point reads; paired collection overhead delta p95 3.4 ms vs the 25 ms S29.16 target with zero incomplete records; real-model spot check (n=3) within run noise — the release-to-insert paired pilot with a human speaker stays pending, honestly labeled.

## /review-2 → /address record

`/review-2` ran two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 3 corroborated criticals, 8 warnings, 11 suggestions after dedup. Highlights: the collector's observation sink was set on the main thread and read on the worker, mis-joining cleanup evidence under the app's supported overlapping dictations; the `log_transcripts` persistence branch was dead code (transcripts silently unretained under default config); daily retention would have destroyed the lossless legacy import after 30 days with re-import unable to restore it.

`/address` fixed every critical and warning in-session (worker-thread context binding with a new overlap regression test; collection-gated transcript persistence; legacy class exempt from prune; event-writer hardening — closed-file ValueError, bounded spill, own-prefix retention, drop counters, 0600 creation; app-path guards so store/evidence failures never drop a dictation; envelope hygiene — no embedded transcript text, prompt-less observations skipped, unambiguous cleanup-proposal join, on_failure quarantine, Transcriber decode constants; store migration v2 indexes, purge-before-unlink ordering, atomic composite import ops closing the crash window between artifact write and bookkeeping; importer identity hashed from the verified backup bytes; viewer fixes). Three suggestions were deferred with reasons (latest-example recency guard; unused Harness.close; sqlite-file mode TOCTOU window) — recorded in results.json `review_loop.findings_deferred`. **No findings rejected as incorrect.** Post-address, every suite was re-run green, the real import re-verified idempotent, the live store migrated to schema v2, and the benchmark re-run on the fixed code. (The /address skill's per-fix commit+push cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's explicit constraints.)

## Collection enablement state (never inferred from documents)

- **The installed /Applications/LocalFlow.app is untouched and still runs the pre-M02 code**: it has no collector, no consent table, no events. Training collection is therefore NOT enabled on the installed build; nothing about the installed app's data behavior changed this milestone.
- The live `v2.db` consent table is empty ⇒ collection reads **disabled** by construction. Enabling requires the rebuilt app (or a dev run) and the menu toggle — building the capability did not change any installed data setting.
- No evidence has been captured yet (first observed capture time: none; the only training-example-producing runs were synthetic/model-backed tests in temp directories).

## Human verification (pending; steps in results.json)

1. UTC/local diagnostic-view inspection of one dictation + reopen check that old analytics/settings survive.
2. The five-dictation live collection pilot (enable → dictate ≥5 → mark/exclude → inspect the audio/stage join → pause → confirm no new payload). This is the earliest collection-ready checkpoint (AC05's automated half is delivered and model-backed).

## Limitations / unresolved provenance

- AC05's live pilot and the release-to-insert latency pilot (M01 carry-over) need a human speaker; both recorded pending, never assumed.
- The installed app predates M02: until the user rebuilds (not authorized this session), events/store/training exist only in dev runs and via the import CLI.
- `retention_metadata_days` is accepted config but job-row pruning is deferred to the Hub milestone (M09); artifacts/audio/transcript/training retention are active.
- M02 capture honestly omits (with reasons): pre-decode context (M06), typed normalization (M04), word timestamps/confidence/n-best/logprobs (`not_captured_at_stage`), bounded outcome observation (M08). Historical log/audio associations stay unverified candidates — no gold labels invented.
- Unresolved from M01, unchanged: producer source of stats/dictionary/transforms (deleted modules; interfaces recovered in the baseline manifest); producer of `instance.lock`.

## Privacy

Nothing private entered Git: committed artifacts carry hashes, counts, locators and synthetic fixtures only; `tests/v2/test_baseline_manifest.py`'s extension + content-marker scan passes over every new subdirectory; live data, verified backups, snapshots and the private evidence root stay under `~/Library/Application Support/LocalFlow/` and `~/Library/Logs/`, outside the repository. Events are content-free by construction and the redacted export is tested against planted secrets.

## Human verification (completed 2026-09-22)

Both pending checks were executed by the user on the dev pipeline and
passed; evidence and timestamps are in `acceptance/M02/results.json`
(`human_verification` + `M02-AC05`). Notes for the record:

- The first dev launch from the ZCode-hosted terminal produced silent
  captures (macOS microphone TCC attributes to the hosting app, which
  cannot be granted); the event stream isolated this precisely
  (`model.loaded` + `nearly_silent_recording` + `empty_transcription`).
  Launching from Terminal.app (which holds the mic grant) resolved it.
  This is a dev-run-only consideration: the installed bundle has its own
  grant.
- Pilot outcome: five dictations while enabled → five joined examples
  (envelope artifact_ids resolve to float32 audio with speech-level
  signal plus text/proposal/applied artifacts); one marked correct
  (revision outcome.correctness=correct), one excluded; pausing captured
  nothing (verified by the user's before/after queries and the consent
  history); a later deliberate re-enable resumed capture — opt-in
  semantics held throughout.
- Redaction re-verified post-pilot: 93 distinct spoken words checked
  against a fresh redacted export — zero word-boundary matches, `detail`
  absent from every record. Four apparent hits were substrings of JSON
  field names, not transcript leakage.
- `afplay` on an artifact WAV failed only because the pasted long path
  split into multiple shell arguments; the float32 WAV format and signal
  were verified programmatically (afplay supports the format).
- STATUS is now `verified_complete`. The M01 release-to-insert pilot
  remains the only pending human item overall (M15 gate).

## M03 eligibility

**Dependency-eligible: yes.** M02 delivered the dated event stream, persistent job/artifact store with stable IDs, the import contract M03's capture journal and worker restarts build on, and the evidence collector M03 strengthens (sample-accurate original-audio retention and crash handling per S29.5/E19.1). Pending human checks (the live pilot and view inspection) do not gate M03's automatable work.
