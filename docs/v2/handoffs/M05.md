# M05 Handoff — Scoped vocabulary and dictionary management

**Milestone:** M05 · **Requirements:** LF-R08, LF-R28, LF-R29 · **State:** `implemented_pending_human`
**Base commit:** `9d53b4fdebcbfbdf964113af66e5d36f57250f14` (branch `main`, remote `scalinity/LocalFlow`)
**Tree at start:** clean (M04 committed at `9d53b4f`; verified clean before changes). No other worktrees; no other session owned any file M05 touched.
**Tree at end:** M05 adds `localflow/v2/{vocabulary,vocabulary_store,dictionary_panel}.py`, `tests/v2/vocabulary/` (4 suites + `fixtures_vocabulary.json` 40, `fixtures_hint_matrix.json` 40 labeled bases, plus a 120-phrase generated-in-test negative corpus), `scripts/v2/benchmark_m05.py`, `docs/v2/{contracts/vocabulary.md, acceptance/M05/, benchmarks/20260922-035958-m05/}`; modifies `localflow/{app,config}.py`, `localflow/v2/{__init__,store,training,capabilities}.py`, `localflow/v2/normalize/{syntax,engine,policy,span_types}.py`, `tests/v2/{test_baseline_manifest.py, fixtures/manifest.json, storage/test_store.py, lifecycle/test_audio_fidelity.py, lifecycle/test_worker_live.py}`, `docs/v2/{STATUS.json, contracts/{INDEX,store,asr_hints,training_evidence,normalization}.md}`. The worker protocol, capture journal, cleanup adapter, installed app and all M01–M04 artifacts are otherwise untouched (no build authorized).

## What shipped

1. **The vocabulary domain (Spec S11, EV-07).** `localflow.v2.vocabulary`: the entry model (canonical, language, kind term|skill, matching mode, scope, priority/pin, usage, origin, enabled/approved, verification label `explicit|context_supported|suggested`, per-entry revision, per-alias approval); five scopes (global/app/site/profile/workspace) with deterministic precedence workspace > profile > site > app > global; the immutable `VocabularySnapshot` (approved+enabled+in-scope match index over explicit aliases plus each entry's own canonical for case preservation — `mlx` → `MLX`; same-scope collisions masked and surfaced as conflicts, never resolved by insertion order; content-hashed `m05:` revision); `sandbox_phrase` and `preview_entry_conflicts` for the UI.
2. **Engine integration (layer 5, contracts/normalization.md).** `grammar_vocabulary` in the M04 engine: token-boundary phrase matching on token cores (edge punctuation survives), longest-valid-phrase within the layer, already-canonical spans emit nothing and claim the span (idempotence shield against shorter-alias drift), blocked in quote/escape zones, URLs safe by tokenization. Every proposal carries `rule_id` (approving entry) and its verification label — additive `rule_id` field on Proposal/EditRecord/RejectedProposal, JSON round-trip tolerant of old ledgers. Dictionary-scoped skills populate `registered_skills`, closing M04's inert unknown-skill gap. `grammar_identifiers` received the same token-core span fix.
3. **Persistence (store schema v3, additive).** `vocabulary_store.VocabularyStore` over the single-writer store: versioned entries with append-only history and a state counter; one-canonical-per-scope enforced case-insensitively (caller-thread probe with content-free errors + NOCASE unique index); the seven legacy terms adopted from the M02 legacy artifacts; Claude/Claude Code seeded as visible unapproved profile-`coding` suggestions that never rewrite text until approved and stay dismissed once disabled; idempotent JSON import/export that never fabricates usage; `record_hits` for applied matches (approval checked at match time under the frozen revision; usage excluded from invalidation so hits never force rebuilds). `Store.submit(fn(db))` is the sanctioned writer-thread entry point; the torn-write repair path covers the v3 tables.
4. **Pipeline wiring (app.py).** At hotkey-down — after the overlay shows, so selector work never delays visible feedback — the app freezes per job: vocabulary policy (dictionary skills registered), context (snapshot in `ContextSnapshot.vocabulary`) and a HintSet; in-flight jobs keep their revision across edits (AC03, tested live mid-flight). Applied approved matches record hits after normalization; the coordinator uses the job's captured policy/context. A store failure degrades to vocabulary-off with an event — never a startup crash, never a dropped dictation (tested).
5. **Hint sets and the selector (S30.1, EV-18).** `RelevantVocabularySelector` ranks pin → scope → recency → frequency → priority (ties by entry id; order precomputed at snapshot construction) and emits an immutable `HintSet` (ordered terms with scope/source/score — a score, not a probability; budget omissions with `budget_limit`; content-hashed id; `hint_term_limit` config, default 100). Frozen pre-decode and stored before recognition when collection is enabled — later corrections never relabel original hints (S29.11). `hint_disposition(manifest, hint_set)`: offered-but-ignored under the still-unqualified adapter (zero offered ⇒ nothing ignored — honest false/null); `asr_hint_request_fields` returns None until an adapter+checkpoint+runtime is separately qualified (the extension point; its future shape pinned by a synthetic-manifest test). Post-ASR repairs are ledger edits with rule ids, never decoder hits.
6. **Evidence (S29.4 context family).** `on_hint_set` writes the frozen set as a lease-governed artifact pre-decode and fills the envelope's `context` block (ids/revisions/counts/disposition only); the normalization block gains `vocabulary` (snapshot revision + applied rule ids). Failure to retain emits an event.
7. **Management surface.** `localflow/v2/dictionary_panel.py`: a deliberately simple AppKit panel (search, numbered listing, add-with-conflict-preview, approve/disable/pin/delete, phrase sandbox with scope-labeled suggestions) over the public store APIs — reusable by the Hub (M09). Menu: Dictionary…, Import/Export Dictionary JSON…. Construction is smoke-tested; interactive use is the pending human check.

## Contracts and decisions

- New `contracts/vocabulary.md` (entry model, scope precedence and conflict masking, snapshot semantics, matching rules, store schema v3, HintSet/selector, consumers, evidence, management surface, documented limitations, M06 dependencies). `contracts/INDEX.md` row + decision note; `contracts/store.md` M05 additions; `contracts/asr_hints.md` M05 live status; `contracts/training_evidence.md` context-family live status; `contracts/normalization.md` layer-5-fed status. No frozen identifier changed; registry unchanged (33/22); no ADRs — no spec deviation accepted.
- **How the dictionary feeds the policy (settled, per the kickoff):** the app rebuilds `NormalizationPolicy` and the context only when the store's revision counter changed (skills hash into `policy_revision`; terms flow through the immutable per-job snapshot), and captures the trio into the job at hotkey-down — construction is per-dictation-cheap and cached, in-flight jobs hold their frozen objects, and AC03 holds by object identity rather than snapshot invalidation.
- **Usage is ranking statistics, not matching state:** `record_hits` does not bump the state counter and the snapshot revision hash excludes usage — an applied hit never forces a snapshot rebuild; selector recency/frequency see the last-edited snapshot. Chosen to keep hits off the hot path (a 10k-entry rebuild costs ~125 ms).
- **A lone approved wrong entry rewrites its own alias** (measured in the matrix: distractor flips 40/40 under distractor-only); the protection is same-scope masking when the true term exists. An approved rule does what it says — reported, never hidden.
- **Pre-M06 scope honesty:** the live app filters with no destination context (global entries only, hint set offers global entries only); workspace/profile/site/app resolution waits for M06. The sandbox labels out-of-scope suggestions rather than advertising rewrites that cannot fire.

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M05/results.json`. All cwd = repo root, all exit 0 unless noted:

- **Pre-change baselines:** 4× M01 suites, event writer, store, import, collector, 4× M03 lifecycle suites, 4× M04 normalization suites, stuck overlay — all pass; live pipeline 2/2 (model-backed); cleanup 51/51 (model-backed, 44.8 s).
- **Post-implementation and post-review:** the four new M05 suites — `test_vocabulary_matching.py` (40 EV-07 fixtures exact incl. SEED-13/14 mixed contexts, 13 negative controls, 120/120 generated ordinary phrases with zero vocabulary triggers and a self-check that no alias appears, AC04 attribution, idempotence with the escape corner declared, dictionary-skill wiring, skill/typed-grammar outranking, M04 protection regression, sandbox, 4-kind conflict preview), `test_vocabulary_store.py` (schema v3, AC01 adoption/reopen/idempotence, versioned history, AC03 store half, hits, JSON round trip, case-insensitive duplicates, torn-write repair, immutability), `test_hint_selection.py` (deterministic ranking, limits/omissions, unsupported-adapter honesty, offered-vs-applied provenance, wrong workspace, the 40×4 matrix with exact denominators, no post-answer leakage, consumer set, request-shape pinning), `test_vocabulary_pipeline.py` (real coordinator: vocabulary between ASR and cleanup, hits recorded, frozen pre-decode hint set + honest disposition in evidence, vocabulary evidence block, AC03 live mid-flight, store-failure degradation, panel construction smoke) — plus every pre-change suite re-run green, `test_worker_live.py` green after its assertion was updated to the M05 disposition semantics, and both model-backed suites green post-review (live 2/2; cleanup 51/51, 44.9 s).
- **Benchmark** (`docs/v2/benchmarks/20260922-035958-m05/m05.json`, post-review code, no model call): 10,000 stored entries — selector retrieval p95 **5.142 ms** (scoped 5.332 ms), normalize-with-loaded-dictionary p95 **2.413 ms** (500-word prose), all vs the 25 ms budget; prompt subset 100 offered / 9400 omitted (`budget_limit`); cold snapshot build p95 126.9 ms, cached per revision.

## /review-2 → /address record

`/review-2` ran two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 1 critical, 7 warnings, 14 suggestions after dedup. The critical: `update_entry` crashed on the `(alias, approved)` tuple format its own callers `approve_entry` and `import_json` pass — the one-click Approve flow and alias-changing imports could never land (every suite missed it because neither path was exercised).

`/address` fixed the critical and every warning in-session with regression tests: tuple-alias acceptance (approve/import tests); the stale `test_worker_live` assertion; aliases-only updates now version the entry (empty updates are honest no-ops, None rejected); sandbox suggestions scope-labeled (and never shown inside quoted literals); alias rows follow updated languages; hits no longer invalidate the cached snapshot (usage excluded from the revision hash); position-driven first-word matching with precomputed orders plus a normalize-with-dictionary benchmark measurement (this also fixed a machine-noise gate crossing: scoped retrieval measured 25.3 ms on one post-review run → 5.3 ms after the structural fix); duplicate rejections moved to a content-free caller-thread probe with a NOCASE unique index backstop (event-log hygiene); tests for every previously untested public API and a real immutability check. Eleven suggestions applied (`submit(fn(db))` keeps `_db` private, v3 tables in the repair set, skill conflicts carry entry ids, panel read guards, `on_hint_set` failure event, engine tokenizer reuse in the sandbox, import docstring/keys, dead code removal, request-shape pinning). Three deferred with reasons (deep-frozen containers; a shared punctuation helper; ranking freshness vs cached snapshot) — recorded in `results.json` `review_loop.deferred`. **No findings rejected as incorrect.** Post-address, every suite and both model-backed checks re-ran green and the benchmark was re-run on the fixed code. (The /address per-fix-commit cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's explicit constraints.)

## Human verification (pending; steps in results.json)

`native-m05-dictionary-trial`: dev-run from Terminal.app (ZCode-hosted terminals get silent mic captures under macOS TCC; one instance at a time). Inspect the Dictionary panel (legacy terms adopted; Claude/Claude Code suggested, unapproved); create a workspace-only Claude alias and exercise it through the sandbox (scope-labeled) and live dictation (global terms apply today; the workspace-scoped live half waits for M06 — the sandbox half is runnable now); disable and confirm behavior reverts; record steps/expected/actual. Native checks also remain pending from M03/M04/M01 as recorded in their results files; none gate M06.

## Limitations / unresolved

- Documented in `contracts/vocabulary.md`: live scope resolution waits for M06 (global-only until then); one matching mode (`phrase`), and an entry cannot opt out of its own canonical matching; the literal-escape idempotence corner extends to alias-word escape objects (LF-VOC-027 declares it); a lone approved wrong entry rewrites its alias (matrix-measured).
- The installed /Applications app still runs pre-M02 code (no build authorized); M05 exists in dev runs and tests. The live `v2.db` migrates to schema v3 on the first dev run (additive, backup-gated).
- Unknown from M01–M04, unchanged: producer source of stats/dictionary/transforms; `instance.lock` producer; the M01 release-to-insert pilot (M15 gate).

## Privacy

Nothing private entered Git: committed fixtures are synthetic (origin-tagged); the envelope's context/vocabulary blocks carry ids/revisions/counts only — the frozen hint set's terms live in a lease-governed store artifact; duplicate-rejection events are content-free; the store error channel never receives canonical spellings; the baseline-manifest privacy scan passes over every new path; live data stays under Application Support / Logs, outside the repository.

## M06 eligibility

**Dependency-eligible: yes.** M05 delivered the scoped-vocabulary interfaces and hint-set plumbing M06's context snapshots feed (`ScopeContext`, the per-job snapshot/policy/context capture, the `context_snapshot_id` request field awaiting M06's value). Pending human checks (the M05 dictionary trial; M03/M04 native items) do not gate M06's automatable work.

## Remediation addendum — 2026-09-25 (cloud session, post-audit)

**Input:** read-only audit “LocalFlow V2 — M05 Read-Only Deep Audit” at `267d1c25910328f72afff7096eb2a442e96a5174` (verdict C; 2 Critical, 7 High, 5 Medium, 1 Low, 5 test gaps, 6 design concerns; report sha256 `1df4ca85…f260`) and its adversarial corpus (226 cases / 66 families, all `NOT_RUN`; sha256 `c9c51adc…3088`, committed verbatim as `tests/v2/vocabulary/m05_audit_corpus.json`). **Inherited base:** `origin/main` = `267d1c2` (unchanged since the audit; accepted M01 `3db7406`, M02 `bfbc63c`, M03 `3e0d1ab` and M04 `4ef219c` verified as ancestors; schema 11 unchanged). **Branch:** `claude/m05-vocabulary-remediation-kgi2kl`, created from `267d1c2`. **Commits:** `fff94eb` tools + base evidence (no production change) · `a164456` **first-pass production repair (frozen for review)** · `51e5cb6` author findings A1/A2 · `fac2a0b` review-round fixes · `01a1f3c` real-selection cost + benchmark cohorts — **final production code `01a1f3c`** · `4adda7d` hit-accounting mutation witness (test only); every later commit is evidence and documentation only. **Environment:** Claude Code cloud container (Linux x86_64), CPython 3.13.12, SQLite 3.45.1, declared non-native shims (`tests/v2/lifecycle/native_shims.py`) — not the reference Mac; nothing native, model-backed, installed-app or Application-Support related was executed or is claimed. **State:** `CLOUD_REMEDIATION_COMPLETE_PENDING_LOCAL_VERIFICATION`.

Every finding was reproduced first on a clean worktree of `267d1c2` by `scripts/v2/m05_remediation_repro.py` (synthetic terms, temporary stores; app probes drive the real `AppDelegate` hotkey-down → `_vocab_job_state` → M06 finalize → coordinator path under the declared shims): **21/21 reproduced** (20 findings + the 02b sibling) — `acceptance/M05/remediation/repro_base_267d1c2.json`, re-confirmed 21/21 with the first-pass-aware probe revision (`repro_base_267d1c2_probes_v2.json`). At `a164456`: 0/21; at the final code: **0/21** (`repro_final_4adda7d.json`). No finding was refuted or already fixed.

### Dispositions

| Finding | Disposition | Observed on `267d1c2` | Repair | Regressions (fail on `267d1c2`) |
|---|---|---|---|---|
| 01 multiword alias crosses a clause delimiter (Critical) | reproduced | “clod, code”, “clod. code”, “clod\ncode” and 11 more separators → `Claude Code` | an alias owns CONNECTED word cores only; every clause/line separator is a barrier; edge punctuation survives | `test_01_…` |
| 02 failed refresh reuses another job's scope (Critical) | reproduced (+02b) | an app.B job after a refresh failure got app.A's `ask Claude now`; a selector failure blocked the M06 upgrade; a capture-failed job reused the cached scope | proven cache key; the failure path rescopes THIS job from the last good frozen entries or runs vocabulary-off; unscoped default for failed or invalid identities (02d); the selector is independent of the upgrade; M10 finalize never rebuilds on the app cache (02e) | `test_02_…` (4, app), `test_02b/02c/02d/02e` |
| 03 canonical shield order-dependent | reproduced | “red status page” → `red Status Page` → `Orange Page` on pass 2 | claims collected for the whole text and arbitrated with proposals (re-keyed in the review round, R1/R2/R17) | `test_03_…`, `test_R1/R2/R17` |
| 04 snapshot not deeply immutable | reproduced | `scope_ctx`, `match_index`, `revision`, conflict dicts rebindable; a caller's alias list mutation changed a later rescope | sealed snapshot, read-only views, entry-owned alias tuples | `test_04_…` |
| 05 HintSet content changes under one id | reproduced | omissions and scope mutable under the same `hint_set_id` | deep-frozen, content-bound id, checked constructor; scalars only (R13) | `test_05_…`, `test_R13` |
| 06 read-modify-write outside the writer | reproduced | approve lost a concurrent alias; duplicate next revision; invalid scope committed; orphan aliases after an update/delete race | read-merge-validate-version inside ONE writer op; `expected_revision` compare-and-swap; typed caller errors | `test_06_…` (5; turnstiles, both orders, no sleeps) |
| 07 malformed booleans coerced | reproduced | 28 malformed cells admitted; the string “false” became approval | strict admission at every boundary; list/tuple alias containers (R7); strict `hint_term_limit` (R12) | `test_07_…` (2), `test_R7`, `test_R12` |
| 08 panel approval retargets | reproduced | Alpha approved while Beta was selected | selection is a stable entry id; actions bind to the revision the user selected (Q3) | `test_08_…`, `test_Q3` |
| 09 dictionary skill loses its entry | reproduced | skill edit `rule_id` None through the M10 registry | provenance registry → policy → skill edits; hits for applied dictionary skills | `test_09_…` (1 + 2 app) |
| 10 unsorted aliases break re-import | reproduced | identical re-import `updated` every time | order-insensitive alias identity on STORED language (R3) | `test_10_…`, `test_R3_R4` |
| 11 partial import behind an error | reproduced | a duplicate identity in the file left a partial commit | whole-file transaction; ASCII NOCASE identity (D6); duplicate aliases refused up front (R5) | `test_11_…`, `test_R5` |
| 12 alias language not preserved | reproduced | explicit override lost; inherited language drifted | NULL inherits, a value is an explicit override; one-time migration of legacy materialized rows (R4) | `test_12_…`, `test_R3_R4`, `test_R4_…` |
| 13 sandbox/preview ≠ live | reproduced | panel tested global only, unlabeled; inactive masks shown as active; suggestions claimed protected rewrites | explicit, labeled sandbox scope; preview `active` flags; engine-decided suggestions; fresh preview read (R14); Hub preview on the unscoped default (R15) | `test_13_…` (3 + 1 app), `test_R14`, `test_R15` |
| 14 hint retention failure = non-capture | reproduced | a write/lease failure read as ordinary non-capture | `retention_write_failed` with the failed step; id/counts/disposition kept | `test_14_…` |
| 15 approving rule not reconstructable (test gap) | reproduced — a real evidence gap | scope, alias approval and entry revision of a deleted applied rule unrecoverable | lease-governed applied-rule manifest per job, bounded to applied rules | `test_15_…` (2), `test_R8_applied_rule_…` |
| 16 benchmark accepts no work (test gap) | reproduced | identity matcher, wrong-scope selector and skip-every-fourth exited 0 (scripted clock) | work validity before timing (exit 2), independent ranking oracle; fresh-selection gates (R9) | `test_16_…`, `test_R9` |
| 17 oracles thinner than the system (test gap) | test_gap_only | inherited suites green while 01/02/03/06/07 coexisted | new suites, corpus runner, mutation harness (R8/R10) | `test_17_…` (2; the budget matrix pins behaviour already correct on base) |
| 18 torn-table repair on an empty store (test gap) | reproduced, **narrowed** | a lost entries table repaired empty, seeded over, no signal. **Narrowed:** an index repair over duplicate rows makes the store refuse to open — M02's repair path, unchanged | `integrity_report()` + app refusal; orphan alias rows count as vanished entries (R11) | `test_18_…` (1 + 1 app), `test_R11` (2) |
| 19 qualification = shape only (test gap) | reproduced | a bare `supported` flag, a missing checkpoint and a mismatched runtime returned request fields | identity-bound qualification; self-attestation hardening (Q1); consistent disposition (A2) | `test_19_…`, `test_Q1`, `test_A2` |
| 20 stale active statements (Low) | reproduced | pre-M06/M07 statements in `vocabulary.md`, `asr_hints.md`, `test_hint_selection.py` | active contracts describe the connected system (history stays here) | `test_20_…` |

**Adjudicated policies** (explicit in `contracts/vocabulary.md` → “M05 remediation”): D1 language is informational, never a match filter · D2 an explicitly approved rule does what it says (same-scope masking is the safety) · D3 suggestions and masked canonicals may be offered as context, never rewrite or become cleanup alias pairs · D4 pin-first ranking and frozen usage between edits · D5 scope identity = canonical + kind + canonical scope value · D6 whole-file import, ASCII NOCASE identity, omitted fields keep stored values · D-M1 a masked longer alias does not reserve its span · delete re-seeds a suggested term, disable dismisses it.

**Guardrails honored.** The dictionary was not disabled, made global, stripped of aliases or scopes to pass; `retry_unscoped_default`, M04 barriers/claims/arbitration, M06 identity values, M07 approved-only alias pairs and M10 manifest-skill behaviour are preserved; schema 11 unchanged.

### Independent review round

**Independence method.** The frozen first pass `a164456` was reviewed by a separate subagent started with an empty context and only the audit, the corpus, the contracts, the diff `267d1c2..a164456` and a written brief, in its own read-only detached worktree at `a164456` (probes and outputs in its own scratch directory; no repository write, commit or push; it disclosed four git-ignored `__pycache__` directories its first probe created in its worktree and removed at once). It did not see the author's notes. Every allegation was re-run by the author on a separate worktree at `a164456` before any fix: all 22 probes reproduced (`review_round_probes_first_pass_a164456.json`); the probes are committed under `remediation/review_round_probes/`. Structured record: `remediation/review_round.json`.

| ID | Defect left by the first pass | On `a164456` | Fix (`fac2a0b` unless noted) |
|---|---|---|---|
| R1 (High) | claims keyed by canonical length: a winning alias longer than its canonical lost on pass 2 | “reddish status page” → `Red Status page` → `Red Orange Page` | claims keyed at the entry's longest matchable form |
| R2 | a claim beaten by a longer proposal still suppressed a third | `aaaaaa Bb Cc dd` → `Zz Cc dd` → `Zz Yy` | one greedy pass over claims + proposals; only surviving claims suppress |
| R3 | import lost an explicit alias language equal to the old entry language; repeat import `updated` | override NULL, `updated: 1` | identity on the stored language |
| R4 | legacy-materialization rule applied to new explicit overrides | explicit `en` → inherit | one-time migration, then no heuristic |
| R5 | duplicate/case-variant aliases reached the writer | `write_failed` + ERROR event; `Clod`/`clod` as two rows | refused up front (`duplicate_alias`) |
| R6 | a global entry could carry a scope value (second identity; preview ≠ committed) | second global `Claude` accepted | global takes no value |
| R7 | a mapping alias container dropped its flags | `{"clod": False}` → approved alias | list/tuple only |
| R8 | five behaviour-changing mutations survived every tool | 5 survivors | harness runs corpus + 3 suites; witnesses; `4adda7d` adds the hit-accounting witness |
| R9 | gated selection timed memo hits | a 150 ms-per-selection implementation passed | fresh-snapshot cohorts gated; `01a1f3c` cut real selection to ~10 ms p50 at 10k |
| R10 | a crashed control reported green | `control_green: true` | crash / missing result is never green |
| R11 | entries + history lost → orphans ignored, dictionary booted | vocabulary on, seeded | orphans of never-deleted entries = vanished |
| R12 | `int()` coercion of `hint_term_limit`; a bad value turned vocabulary off | `True→1`, `0` → off | strict, default 100 with `vocabulary.config_invalid` |
| R13 | nested mutable values under one id; memo poisonable | two contents, one id | scalars only; read-only memo view |
| R14 | add preview read the stale listing | contender unseen | fresh store read |
| R15 | Hub preview used the last dictation's scope | output depended on the last app | unscoped default, `scope: "global only"` |
| R16 | case-variant alias order by insertion | two orders | NOCASE then binary |
| R17 | shadowed/masked canonicals claimed nothing → pass-2 drift | `clod → Cloud → Claude` | exact canonical spellings claim |
| R18 | U+001F not a barrier | `clod\x1fcode` rewritten | barrier; Zs spaces stay benign |
| R19 | qualified disposition contradictory | `ignored: false` + reason | `51e5cb6` (A2) |
| Q1 | self-attested qualification | baseline evidence, `.` evidence, blank checkpoint qualified | refused (registry declined — see below) |
| Q2 | last-good rescoping resurrected a disabled rule | disabled rule applied + hit | stale last-good → `vocabulary_off_stale_last_good` |
| Q3 | click-time revision approved an unseen alias | unseen alias approved | selection-time CAS |
| Q4 | omitted import fields overwrote with defaults | skill de-approved, unpinned, re-kinded | omitted fields kept |
| Q5 | scope values byte-exact | trailing slash / host case / padding / bundle case inert | canonical comparison form; `ScopeContext` keeps M06's exact values |
| Q6 | aliases + history lost together are silent | report all zeros | **declined** — no independent record exists; declared residual |

Author findings while the review ran: **A1** the panel listing said “click a line to select” (selection is by line number in Test phrase) and **A2** (= R19), both fixed in `51e5cb6`. Per-test results of the 31 review-round regressions: `regressions_first_pass_a164456.json` **25 fail / 6 pass** (the six are five mutation witnesses and one residual pin, which document behaviour that was already correct) and `regressions_final_4adda7d.json` **31/31 pass**; the remediation suites 28 + 12 pass at the final code (one first-pass app case, `test_08` changed-since-selection, is the intended Q3 edit). **Fresh family REV-FAM-1** (reviewer-authored, expectations fixed before running; app scope on a neighbouring bundle id, a barrier, a suggestion, a store failure, a missing identity): 6/6 on `a164456` and 6/6 on the final code. **Narrowed/declined:** R2's case-dependence is adjudicated (an already-canonical spelling is keyed by its entry); Q1's external qualification registry is declined (the manifest is in-repo code, so qualification is a reviewed code change either way); Q6 is a declared residual.

### Adversarial corpus, mutations and metamorphic checks

`tests/v2/vocabulary/m05_corpus_runner.py` (runner `m05-corpus-runner-1`) judges every case with oracles that never call the code under test beyond the path being judged, writes results keyed by case/family/SHA/runner version, and never edits the corpus. It is development evidence visible to the remediation, not a blind holdout.

| Code | pass | fail | declared residual |
|---|---|---|---|
| base `267d1c2` | 159 | 67 (21 families) | — |
| first pass `a164456` | 224 | 0 | 2 |
| final `4adda7d` | **224** | **0** | **2** |

By role at the final code: stateful 30/30, mutation 12/12, metamorphic 10/10, challenge 46/46, negative 45/45, positive 30/30, reproduction 29/29, fault 7/7, characterization 15 + 2 residual. Residuals: TOKEN-001 “(clod code)” and TOKEN-003 “'clod code'” — M04 attached-punctuation tokens are not word tokens. **Mutation harness** (`scripts/v2/m05_mutation_check.py`, corpus + remediation + app + review-round suites, each mutation proven applied): control green, **18/18 mutations killed**, 0 errors (`mutation_final_4adda7d.json`); the first-pass corpus-only harness killed 8/8 (`mutation_first_pass_a164456.json`).

### Contracts changed (and why)

`contracts/vocabulary.md` → “M05 remediation (2026-09-25)”: occurrence ownership and barriers, canonical claims (strength, greedy arbitration, residual), masked longer phrase, deep immutability, strict admission, scope values, authoritative read-modify-write, import (transaction, identity, duplicate aliases, omitted fields, order), alias language + migration, sandbox/preview/Hub, panel selection, skill provenance, applied-rule manifest, failure paths, integrity, qualification, retention, adjudications. `contracts/asr_hints.md`: identity-bound qualification, self-attestation refusals, the qualified disposition. `contracts/training_evidence.md`: hint retention failure and the applied-rule manifest. `contracts/normalization.md`: U+001F barrier; Zs spaces benign. Evidence-envelope changes are additive.

### Portable verification (tested SHA `01a1f3c`; tests at `4adda7d`)

Interpreter CPython 3.13.12, SQLite 3.45.1, declared shims. Per-file exits and counts: `remediation/portable_tests_final.json`; whole-tree sweeps `sweep_base_267d1c2.json`, `sweep_first_pass_a164456.json`, `sweep_final_01a1f3c.json`.

| Group | Files | ok lines | Non-zero |
|---|---|---|---|
| M05 required (matching 12, store 16, hint selection 9) | 3 | 37 | 0 |
| M05 pipeline (native panel smoke cannot run under the shims) | 1 | 5 of 6 | 1 (base: 0 of 6) |
| M05 remediation (28), app seams (12), review round (31) | 3 | 71 | 0 |
| M04 normalization (numbers 13, syntax 13, short commands 3, corpus 2, remediation 19, review 22, app 6, pipeline 7) | 8 | 85 | 0 |
| M06 context (snapshot 27, pipeline 12) | 2 | 39 | 0 |
| M07 cleanup (engine 18, remediation 36, pipeline 13 — base: 1) | 3 | 67 | 0 |
| M10 profiles/skills (style 9, snippets 9, skills 6, developer 6; profiles pipeline shim-limited as on base) | 5 | 31 | 1 (= base) |
| M14 learning/datasets (classification 7, sampling 3, splits 4, learning 11, profile 9, dataset export 6 from the main checkout) | 6 | 40 | 1 in the worktree sweep (= base; no `.venv` in a detached worktree), 0 from the main checkout |
| M02 evidence/store (collector 11, remediation collector 14, app wiring 4, store 17, remediation store 19, review 8, import 7) | 7 | 80 | 0 |
| M01 privacy/inventory (manifest 5, snapshot safety 19, registry 5) | 3 | 29 | 0 |
| Model-backed (`test_live_pipeline`, `test_worker_live`, mlx suites) | — | — | **not run** (no MLX/models in the cloud) |

**Whole tree.** 85 files: 68 exit 0; the 17 non-zero are the same shim-limited/model-backed files as on the base, with identical counts except the M05 pipeline suite (0 → 5). Improvements vs base: `test_cleanup_pipeline` (the stub bundle id no longer turns vocabulary off), `test_m03_remediation_supervisor` (flaky once under concurrency on base; passes alone on both). Environment notes: `test_dataset_export` calls `<root>/.venv/bin/python` and fails in detached worktrees on base and final alike (6/6 from the main checkout); `test_capture_journal`'s 5 ms max-latency assertion failed once while the author deliberately oversubscribed the CPU and passes alone (3/3) and in all three sweeps. Intended test edits outside the new suites: `test_vocabulary_store.py` (effective alias language), `test_hint_selection.py` and `context/test_context_pipeline.py` (synthetic manifests carry the full qualified identity), `test_m05_remediation_app.py` `test_08` (Q3).

**Benchmark.** `scripts/v2/benchmark_m05.py` (actual work first: exact outputs, rule ids, an independent ranking and a scope-upgrade oracle; exit 2 invalid work, 1 budget, 0 pass; the identity matcher, a wrong-scope selector and skipped edits exit 2). Cloud run at `01a1f3c`, 10,000 entries (`benchmark_cloud_01a1f3c.json`): verdict **pass**, work valid; gated p95 — cached retrieval 0.14 ms, warm/scoped selection 0.003/0.004 ms (memoized per-job path), **fresh selection 19.4 ms, fresh scoped 13.3 ms** (first selection on fresh snapshots, p50 ≈ 10 ms), normalize no-hit 13.6 ms, positive 12.7 ms (500 words, 20 authored hits); informational — cold snapshot 489 ms, cold selection 190 ms, dense hits 40.0 ms, shared-first-word bucket 5.4 ms, overlaps/long aliases 7.2 ms, **scope upgrade + packaging 364 ms against its 75 ms bound** (declared residual at 10k entries in the cloud, as on base and first pass). The same harness on the audited base: `budget_failed` — normalize positive 103 ms, shared-first-word bucket 1.07 s, dense hits 1.26 s, every selection ≈ 12–15 ms. Cloud figures are algorithmic evidence only; the September 22 reference-Mac figures stay historical and describe older code (M05-V003).

### Historical records (2026-09-22)

The September 22 M05 results, benchmark and pending human item `native-m05-dictionary-trial` are preserved unchanged in `results.json` and above. The pending item is carried for the current code, with its live app-scoped half (which waited for M06 then), by M05-V002 under instruction revision M05-r1.

### Pending local verification (`docs/v2/VERIFICATION.html` → M05, instructions M05-r1, code `01a1f3c`)

Front-end edit made after invoking `anthropic-skills:frontend-design`; only the M05 placeholder section and the “Last updated” line changed. **M05-V001** Mac parity of the M05 suites and the audit corpus (real PyObjC; the native panel smoke) · **M05-V002** the Dictionary panel (stable selection, labeled sandbox scope) and native dictation with global and app-scoped entries, a clause-barrier line, disable/enable and an evidence read (applied-rule manifest, hint disposition offered-but-ignored, integrity) — carries `native-m05-dictionary-trial` · **M05-V003** reference-Mac benchmark at 10,000 entries (the scope-upgrade bound is binding there). All start **Not run**; M01–M04 IDs, revisions and results are untouched. Pending entries in VERIFICATION.html do not block starting the next read-only milestone audit during this campaign unless the remediation discovers that an unverified condition makes downstream static analysis invalid.

### Limits

`PENDING_LOCAL_VERIFICATION`: the AppKit Dictionary panel, Accessibility/M06 native destination identity, microphone capture, real Parakeet punctuation meeting the barrier rules, real Qwen cleanup, reference-Mac latency. Declared residuals: a cross-entry chain through a canonical (flagged `idempotent: false`); M04 attached-punctuation tokens; M02's unique-index repair over duplicate rows refusing the store; aliases + history loss (Q6); scope upgrade + packaging over 75 ms at 10k entries in the cloud; single/German/CJK quote pairs are not M04 protected zones; a store whose `vocabulary_meta` was lost re-runs the one-time alias-language migration.

### Privacy

Synthetic terms and temporary stores only. No installed app, live dictionary, private transcript, scope path or model cache was touched; committed evidence carries ids, counts, timings and synthetic words; the reviewer's probes and outputs stayed in scratch (only the probe sources are committed).

### Merge readiness

Code: ready to merge into `main` from the cloud's point of view — every confirmed finding is repaired with a regression that fails on the audited base, every independent-review finding is fixed or explicitly declined with a regression that fails on the first pass, the corpus is 224/226 with 2 declared residuals, 18/18 mutations are killed, and no portable or targeted downstream regression was found. Native certification: not given — M05-V002 (real dictation, where ASR punctuation meets the barrier rules and the destination identity is native) is the most informative first check.
