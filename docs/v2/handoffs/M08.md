# M08 Handoff — Safe insertion, clipboard transactions and recovery undo

**Milestone:** M08 · **Requirements:** LF-R12, LF-R28, LF-R30, LF-R31 · **State:** `implemented_pending_human`
**Base commit:** `a8dbeda8b1a1e6ffe70ec4256b6e437a52a49a24` (branch `main`, remote `scalinity/LocalFlow`)
**Tree at start:** clean (M07 closed at `dc3c726`; ORCHESTRATION.html realigned at `a8dbeda`, untouched during the milestone and realigned again at this close-out). No other worktrees; no other session owned any file M08 touched.
**Tree at end:** M08 adds `localflow/v2/insertion/` (`__init__, result, hosts, clipboard, target_lease, validation, record, observation, service`), `tests/v2/insertion/` (`fixture_target.py` + 3 suites), `scripts/v2/benchmark_m08.py`, `docs/v2/{contracts/insertion.md, acceptance/M08/, benchmarks/20260922-131238-m08/}`; modifies `localflow/{app,config}.py`, `localflow/v2/{store,training}.py`, the three shared test harnesses (`tests/v2/normalization/test_normalization_pipeline.py`, `tests/v2/lifecycle/test_lifecycle.py`, `tests/test_stuck_overlay.py` — the paste seam moved from `app_mod.paste_text` to the insertion service, so their recording stubs follow it and still drive the REAL `_insertionDone_`), `tests/v2/vocabulary/test_vocabulary_store.py` (schema assertion `== 3` → `>= 3`: additive v4 supersedes), and `docs/v2/{STATUS.json, contracts/{INDEX,targets,context,training_evidence}.md}`. The worker protocol, capture journal, normalization/vocabulary/context/cleanup domains, V1 cleanup control, installed app and all M01–M07 artifacts are otherwise untouched (no build authorized).

## What shipped

1. **The serialized insertion queue (S18).** `InsertionService` owns one daemon thread; `_finishWithText_` only enqueues — no AX call, pasteboard write or settle sleep ever runs on the UI callback (V1 slept 50 ms inline and posted a racing 0.6 s restore thread). Completed jobs never interleave clipboard transactions: the two-queued-results test proves the op-log order publish→restore→publish→restore.
2. **Target revalidation (the M06 `same_destination` consumer, task 1).** `validate_target` runs immediately before writing on the queue thread: identity (pid/bundle — exactly `same_destination` for full snapshots), window title (the system host resolves `AXFocusedWindow` from the application element), field role, and exact-selection-only for the replacement case; a moved caret never blocks (queued results land in dictation order). `target_changed` routes the artifact to saved history with the text on the clipboard as the one-action paste offer. Unreadable window/role reads degrade to `unavailable` — identity alone governs.
3. **The method matrix with ownership generations (task 2).** Cancelled ⇒ nothing; terminal+multiline+uncertified ⇒ copy-only (`multiline_terminal_unverified` — the guard classifies the LIVE frontmost bundle when no snapshot carried a category); AX-untrusted ⇒ copy-only (V1 parity); settable `AXSelectedText` ⇒ AX replacement (clipboard untouched); else the clipboard transaction: capture generation + supported representations (text/rtf/html/pdf/png/tiff, restored as one item), publish, post, bounded settle (0.6 s; readback polls cut it short), restore only if the pasteboard still holds our generation — a user copy always wins.
4. **Readback-only confirmation (tasks 3–4).** `confirmed` requires the pre-write consistency read plus an owned-range readback equal to the text — never the clipboard. Partial reads stay `posted_unverified` (`readback_partial`, restore safe); pre-existing identical content is `match_ambiguous` — never confirms, keeps ownership so a late paste still inserts the right text.
5. **Target-bound undo + retry reconciliation (task 5).** `undo_last` (queue thread, menu-dispatched) restores exactly our revision only while the range still holds our text — a newer edit degrades to the clipboard offer; no synthetic Backspace exists. `paste_again` reconciles with capped reads (`already_present` / async `repaste_submitted`).
6. **S29.8 bounded observation (tasks 6–7, started here not in M14).** A `confirmed` insert certifies the surface; `OutcomeObserver` (default 30 s, `outcome_observation_sec`) reads ONLY the owned range, re-anchors (±400 cp, deadline-bounded) on outside edits, records in-range edits with lease-governed before/after artifacts carrying exact job/region attribution, and stops with recorded reasons (window_elapsed, focus_lost, field_changed, secure_field_transition, new_dictation [timestamped — only dictations after the window opened], session_locked, target_read_failed). The lock re-arms on wake. Uncertified surfaces report `outcome_observation_unavailable` (`unreliable_target`) and never poll. `no_edit_observed`/`undo_candidate` are never correctness labels or preferences (AC06).
7. **Store schema v4 + evidence.** Additive `insertions`/`insertion_observations` (all writes via `Store.submit`); the envelope's outcome family carries the real state machine via `on_insertion_result` (interim observation block at insert, final via `on_observation_closed`); the legacy `on_insertion` keeps the V1 semantics the M02 suite pins. Recovery menu gains Undo Last Insertion and Paste Last Result Again.

## Contracts and decisions

- New `contracts/insertion.md`; `targets.md` transaction half live; `context.md` consumer note updated; `training_evidence.md` M08 status; INDEX row + decision note. No frozen identifier changed; registry stays 33/22; no ADRs — no spec deviation accepted.
- **Confirmation semantics (settled):** readback of the destination only, two-read rule, ambiguous-preexisting guard; a `confirmed` result on a certified surface is the observation's certification — no hand-maintained app list, but no untested claim either (real surfaces certify through the E10 trial).
- **Delayed paste beyond settle (settled):** on observable surfaces ownership is kept (`readback_pending`/`readback_ambiguous` — a late paste reads the RIGHT text; the user clipboard is the disclosed sacrifice); unobservable surfaces restore per V1 with the residual documented.
- **`CERTIFIED_BRACKETED_SURFACES` empty by decision** — every multi-line terminal insert takes the copy-only offer until a real surface is certified (the pending trial's terminal half).
- **Jobs table unchanged** (the M06 decision held): insertion rows carry the snapshot ids and attribution.
- **`failed` insertions map to job state `saved_not_inserted`** (the jobs state machine has no insertion-failed terminal; the `insertions` row carries the distinction).

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M08/results.json`. All cwd = repo root, all exit 0:

- **Pre-change baselines:** 22 fast suites; worker_live 2/2, fidelity 60/60, literal 20/20, structure 40/40, live pipeline 2/2, V1 control 51/51 (all model-backed).
- **Post-review:** races **13/13**, attribution **10/10**, pipeline **5/5** (through the real coordinator); all 28 fast suites; worker_live 2/2, fidelity 60/60, literal 20/20, structure 40/40, live pipeline 2/2, V1 control 51/51.
- **Benchmark** (`20260922-131238-m08/m08.json`, fixture harness, no model/live app): verification p50 0.002/p95 0.004 ms; AX insert p50 0.339/p95 0.692 ms; clipboard p50 0.369/p95 0.625 ms; UI-callback submit during an in-flight 0.6 s settle 0.002 ms.
- **Live store migrated 3 → 4** (zero rows, legacy untouched). Deviation recorded: the first apply ran without `backup_dir` (additive CREATE TABLE only); a labeled backup was taken immediately after, and `Store` now emits `store.migration_backup_skipped` for unbacked armed migrations.

## /review-2 → /address record

Two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 3 criticals after dedup, 10 warnings, 17 suggestions. Criticals: (1) **CA1** — the multi-line terminal guard was bypassed when the job carried no context snapshot (context off / identity failure ⇒ newlines paste into a shell); (2) **both reviewers** — the session-lock stop signal was a one-way latch (after the first sleep, every observation window died at tick 0 with a false reason); (3) **DB1** — the clipboard path confirmed over pre-existing identical text (no pre-write read) and could restore while the paste was still pending, letting a late paste insert the wrong content.

`/address` fixed all three in-session with regression tests (context-absent terminal guard; lock→wake re-arm; ambiguous pre-existing readback) and every warning: mid-transaction cancel records the real outcome (`insertion.cancelled_after_insert` + envelope — regression test); observer close-race reconciled + try/finally; `paste_again`/`undo_last` moved off the main thread; `AXFocusedWindow` resolved from the app element; unreadable role degrades to `unavailable`; deadline-bounded re-anchor; `PASTE_SETTLE_SEC`/named cap constants; doubled decorator; contract drift (events, field-role rule, guard classification, ambiguous/partial semantics, TOCTOU limitation); backup-skip warning; hook-anchored user-copy test. Suggestions applied: dead code removal, first-match fixture pasteboard, redundant predicate, import style, benchmark label honesty, honest undo recovery values, service-unavailable journal policy. Four deferred with reasons (jobs-table failed-state collapse, multi-item clipboard flattening, substring reconciliation, benchmark approximation) — recorded in results.json. **No findings rejected as incorrect.** Post-address, every suite, all six model-backed checks and the benchmark re-ran green on the fixed code. (The /address per-fix-commit cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's explicit constraints.)

## Human verification (pending; steps in results.json)

`native-m08-insertion-trial`: from Terminal.app — app-switch immediately after PTT release; copy image/rich text while processing; undo after a manual edit; two rapid dictations; multi-line into Terminal vs single-line. This is also the E10 destination-class certification prerequisite and overlaps M01's release-to-insert pilot territory (M15 gate). Earlier pending trials (M07 long-prompt, M06 destination, M05 dictionary, M03/M04/M01 native items) stand in their own results files; none gates M09.

## Limitations / unresolved

- Documented in `contracts/insertion.md`: the clipboard method's inherent publish→target-read window (tens of ms) and the restore check-then-write micro-window (pasteboard atomicity does not exist to close either); delayed paste beyond settle on unobservable surfaces (posted_unverified residual); clipboard-path undo needs settable `AXSelectedTextRange`/`AXSelectedText`; no certified bracketed-paste surface yet; undo inference content-based and weak by design.
- `confirmed` is reachable in production only on readback-consistent surfaces; until the trial certifies real apps, most real inserts stay honestly `posted_unverified`.
- The installed /Applications app still runs pre-M02 code (no build authorized); M08 exists in dev runs and tests.
- Unknown from M01–M07, unchanged: producer source of stats/dictionary/transforms; `instance.lock` producer; the M01 release-to-insert pilot (M15 gate).

## Privacy

Nothing private entered Git: insertion rows and envelope blocks carry ids/ranges/counts/reason codes/pasteboard type names only; observation before/after texts are lease-governed store artifacts under the training-buffer lease; events are content-free; the benchmark JSON carries latencies and counts; the baseline-manifest privacy scan passes over every new path; live data stays under Application Support.

## M09 eligibility

**Dependency-eligible: yes.** M08 delivered the safe-insertion contract M09's Hub/history builds on (retry/copy/paste-again in History can reuse `paste_again`'s reconciliation; the pill's saved-result state maps to `saved_not_inserted`/`target_changed`). M09 depends on M02, M03, M08 — all delivered. Pending human checks (M08's trial; M07/M06/M05/M03/M04/M01 items) do not gate M09's automatable work.

## Remediation addendum — 2026-09-26 (local session on the reference Mac, post-audit)

**Input:** read-only audit “LocalFlow V2 — M08 Read-Only Deep Audit” at `180278ee8a157e7372ba187cfb0106556114293f` (verdict C; 19 defect findings — 4 Critical, 12 High, 3 Medium — and 2 proof gaps; sha256 `32e31ab6…216071`) and its adversarial corpus (125 cases / 25 categories, 18 stateful probes, 9 metamorphic relations, 20 implementation and 6 benchmark mutation definitions — 178 items, all `NOT_RUN`; sha256 `31feb051…3f29`, committed byte-identical as `tests/v2/insertion/m08_audit_corpus.json`). The audit executed nothing. The machine-readable findings register was not supplied; the audit text was the authority. **Synchronization:** local `main` was `180278e` with a clean tree and one worktree (no untracked or dirty work); `git fetch --all --prune --tags` left `origin/main` at `180278e` — no drift since the audit; no fast-forward needed. The accepted foundations were verified as ancestors: M01 `3db7406`, M02 `bfbc63c`, M03 `3e0d1ab`, M04 `4ef219c`, M05 `01a1f3c`, M06 `4388b52` and its R4 closure `46ecd5b`, M07 `52edc47`, M11 `9a049b3` (the replay `1545de9`; the cloud heads `aa03af7`/`1be42ee` are not ancestors, by design). **Branch:** `m08-local-remediation-20260926`, created at `180278e`; pushed, not merged.

**Commits:** `f0af82a` base reproduction, frozen corpus, fail-first regressions (no production change) · `b2cb243` production: authority bound to the validated destination · `4260fe3` test seams, desktop clipboard isolation, adjudications · `846fd2c` production: the `AXUIElementIsAttributeSettable` out-parameter — **first-pass production (frozen for review)** · `a50032c` corpus runner, owned-native suite, mutation checker · `221c172` exact owned range in the native astral case · `6c733fa` contracts · `82de7df` work-valid benchmark, benchmark mutants, F25 grading · `7f27262` first-pass evidence — **frozen first-pass head** · `10802d0` production: author review A1 · `4541652` A1 regression · `ea83bc1` production: independent-review findings — **final production code** · `261bfc7` review regressions, corpus drivers, world title rule · `d4d739a` contract (review round) · `9483d1b` runbook M08-V001–V008 · `75fcb80` MUT-08/MUT-12 re-anchored — **final test/tool head** · `4ea2389` review-round and final evidence · then documentation only (this addendum, the `results.json` block, the runbook cross-reference, the orchestration realignment).

**Environment:** reference Mac — Apple M5 Pro, Mac17,8, 18 CPUs, 48 GiB, macOS 27.0 (26A428), arm64; `.venv` CPython 3.14.7; PyObjC 12.2.1 (Cocoa, ApplicationServices, Quartz); numpy 2.4.6; MLX 0.31.2 / mlx-lm 0.31.3; Accessibility trusted for the terminal; models (the one model-backed check only) Parakeet TDT 0.6B v3 snapshot `ed2b7e8c`, Qwen3-4B-Instruct-2507-4bit snapshot `50d42775`, offline. Other Claude sessions and a LectureLive run were active on the Mac during parts of the session (load 2.0–3.7); the final benchmark ran on battery power. Both are disclosed beside the timings. Sanitized record: `acceptance/M08/remediation/environment.json`. **State:** `LOCAL_REMEDIATION_COMPLETE_PENDING_MANUAL_VERIFICATION` — every automatable, native-local, benchmark and independent-review step ran; eight human checks remain (M08-V001–V008).

### Desktop isolation — the clipboard too

Every suite, the corpus, the mutations and the benchmark ran under `tests/v2/context/run_isolated.py`; every record reports `ax_calls_blocked=0`. The base sweep showed that the runner blocked Accessibility and events but not the clipboard: three app-level suites (`test_scratchpad_hub.py`, `test_insights_hub.py`, `test_transform_remediation_native.py`) reach a copy offer and wrote synthetic text to the live clipboard during an isolated run (measured as the live pasteboard change count moving — a counter, never content). The runner now returns a private, uniquely named pasteboard from `NSPasteboard.generalPasteboard` and counts it (`general_pasteboard_redirected`); every later record shows zero live-clipboard changes. The native suites ran natively against their own helper processes only, reached by pid; no key event reached the desktop.

### Reproduction

Every finding was reproduced first on a frozen worktree of `180278e` by `tests/v2/insertion/test_m08_remediation.py` (the independent world `m08_world.py`: separate app/owner pids, equal-title windows, UTF-16 host ranges, a generation clipboard, a paste consumer bound to the system focus, its own read/effect logs; the real service, validation, capture, observer, coordinator and store): **1/43** at base with no harness errors — the one pass is the AUDIT-02 flank control (the base already reads one element once acquired). The corpus at base: 56 pass / 55 fail / 14 not run; stateful 14/18; metamorphic 6/9 (`corpus_base_180278e.json`). Every negative case that passes at base does so because the behavior is already right there (the M06 identity rule and window element, the historical M08 generation and DB1 fixes, M02's artifact barrier, the base's one-element capture) — recorded as preservation or narrowing, not as weak oracles.

### Dispositions

| Finding | Disposition | Observed on `180278e` (world logs; regression id) | Repair | Remaining qualification |
|---|---|---|---|---|
| 01 validation does not bind the written element (Critical) | reproduced | after validation of A/W1/F1 the focus moved to B: `ax_set_text FB 'dictated'`; to A/W2: `ax_set_text F2`; clipboard variant wrote B via AX (`test_a01_*` ×3) | one owner-verified element bound through the lease; effect-boundary re-check of the destination and (review R3) the authorized selection before write/publish/post | focus change between the check and the effect (residual); M08-V001 |
| 02 capture authorizes A, reads B (High) | reproduced (narrowed) | B's range, string and length read under A's decision (`test_a02_capture_never_reads_other_apps_element`); the flank-drift variant passes on base — the base already reads one element once acquired | `capture_selection` acquires A's own focused element and checks its owner pid; unclassifiable fields refused | M08-V006 |
| 03 readers ignore deny/sensitive policy (High) | reproduced | denied-app insert read range/length/string (`service ignored ['denied_apps']`); undo/repaste read the denied field; an unclassifiable field was read; the app never passed its deny list (`test_a03_*` ×4) | separate read capability from the normalized M06 decision + role/subrole classification; app passes the validated policy; window titles resolved on the element’s owner (review R8) | M08-V006 |
| 04 missing proof keeps destructive authority (High) | reproduced | unreadable live range → `say new now`; strict with AXWindow unavailable or no flanks → replaced (`test_a04_*` ×3) | missing proof refuses; strict needs element AND title, both flanks | — |
| 05 native ranges not end to end (High) | reproduced (portable + native) | owned `(4, 7)` for `X😀Y` after 4 units; astral undo `stale_range` (`test_a05_*`); natively the arity defect below made AX never chosen | ax_range decode, UTF-16 lengths, host-boxed range writes; `AXUIElementIsAttributeSettable` out-parameter (found natively) | native suite F07-C01..C04 |
| 06 equal bytes overstate confirmation (High) | reproduced | AX no-op over `hello` → confirmed; missing pre-read → confirmed; no snapshot → confirmed (`test_a06_*` ×3) | attributable-change predicate; no recorded destination ⇒ never confirmed | F12-C04 native proof |
| 07 unchanged prefix read as consumed (Critical) | reproduced | late consumer pasted `CANARY_OLD_CLIPBOARD`; a queued second job produced `TWO TWO ` and lost ONE (`test_a07_*` ×2) | unchanged or unreadable-at-deadline ⇒ pending (review R2); the pending payload blocks later publications/offers until it resolves or lapses after 5 s (D8, review R1) | unobservable late consumer (residual) |
| 08 undo into another field (Critical) | reproduced | `ax_set_range F2 (0,3)`, `ax_set_text F2 ''` (`test_a08_*`) | undo acts only on the bound element while focused, only after an attributable `match` (review R4), and `undone` needs the length proof (R5) | M08-V003 |
| 09 clipboard replacement loses previous text (High) | reproduced | undo left `say  now` (`test_a09_*`) | replaced selection captured before publication | M08-V003 |
| 10 observer reads another field (Critical) | reproduced | 20+ reads of F2 after the switch; reads after a secure subrole transition (`test_a10_*` ×2) | per-tick bound-element and role+subrole check before any read | M08-V007 |
| 11 re-anchor/changed-length manufacture edits (High) | reproduced | re-anchored onto a pre-existing `red`; failed search read attributed as an edit; after-text `tig` for `tiger`; post-deadline edit attributed (`test_a11_*` ×4) | unique re-anchor that also carries a recorded neighbour (review R7); bounded after-text or abstain; deadline/read failure never an edit | M08-V007 |
| 12 observation without capture consent (High) | reproduced | consent off: before/after artifacts and a row written; through the app too (`test_a12_*` ×2) | capture-time `observation_consent` (ctx.collecting; candidate recorded) | — |
| 13 deletion does not revoke M08 state (High) | reproduced | after delete-everywhere, undo wrote `ax_set_text F1 ''`, repaste and observer kept reading (`test_a13_*`) | flag-only `revoke_job` from the deletion listener; observation rows refused for deleted jobs; no read of a deleted job’s pending destination and no re-cache mid-transaction (review R9/R10) | — |
| 14 no operation identity (High) | reproduced | `_pending 0 → -1`; `onceonce`; two repastes `hellohello` (`test_a14_*` ×3) | operation ids, consumed fence, stale attempts, idempotent coordinator, queued reconciliation | — |
| 15 cancel before avoidable effect (High) | reproduced | AX write after cancel; paste posted after cancel (`test_a15_*` ×2) | checks after validation, before publish and before post | — |
| 16 late exception erases effects (High) | reproduced | `failed/none/transaction_error` after a real write and after a post (`test_a16_*` ×2) | stable insertion id + phase ledger | — |
| 17 repaste AX reads on the UI thread (Medium) | reproduced | 2 AX reads on the calling thread (`test_a17_*`) | whole reconcile+insert queued | benchmark UI cohort |
| 18 close/subscribe gap (Medium) | reproduced | 0 final callbacks (`test_a18_*`) | atomic `subscribe_close` | — |
| 19 snapshot/ack weaker than disclosed (Medium) | reproduced | mixed-generation RTF reported unsupported; posted after a refused publication; (added) a copy after publication was pasted (`test_a19_*` ×3) | capture retry/conflict; acks propagated; ownership before the post | native restore/publish refusals (F09-C04/C05) |
| 20 benchmark proof gap (test gap) | confirmed (test gap) | — | work-valid benchmark + 6 benchmark mutants | — |
| 21 oracle independence (test gap) | confirmed (test gap) | the historical fixture: one field, code points, favourable acks | independent world, corpus runner, owned-native suite, 20 mutants | manual arbitrary-app |


Regression record: `regressions_base_180278e.json` **1/43** · `regressions_first_pass_82de7df.json` 43/43 · `review_regressions_first_pass_7f27262.json` 43/57 (the review round's cases and A1, failing on the frozen first pass) · `regressions_final_9483d1b.json` **57/57**.

**Native qualification found a pre-existing production defect** no portable test could: `SystemInsertionHost.is_settable` called `AXUIElementIsAttributeSettable` without PyObjC's out-parameter, raised `TypeError` inside a broad `except` and answered False for every element — natively the AX method was never chosen and Undo always reported `unsupported_surface`. Fixed in `846fd2c`; the native suite at `4260fe3` fails F07-C02/C03/C04 (undo unsupported, text not restored) and passes after; the benchmark's native cohort at `4260fe3` is invalid in every sample.

**Adjudicated policies** (`m08-policy-v1`, `tests/v2/insertion/m08_corpus_adjudications.json`, stated in `contracts/insertion.md` → “Remediation policy”): D1 read capability · D2 recorded destination · D3 destination binding and the effect boundary · D4 plain destination (M06 D5 carried over; a new live selection is not a caret move) · D5 strict proof · D6 confirmation · D7 bound readback (LF-M08-F03-C05 graded on its invariant: a witnessed AX write confirmed from the written field, zero reads of the newly focused one) · D8 the pending clipboard payload · D9 ownership before the post · D10 operation identity · D11 cancellation linearization · D12 observation · D13 deletion (content-free insertion rows allowed; observation rows never opened for a deleted job) · D14 recovery lifetime (3600 s) · D15 terminal text (CR, CRLF, Unicode separators and control characters are copy-only) · D16 substring reconciliation · D17 native evidence · D18 benchmark cases.

### Independent review round

**Frozen first pass:** evidence/tool head `7f27262`, production `846fd2c`. The author's own review of it then found A1 (below), fixed in `10802d0` before the independent round reported. **Method:** a fresh-context agent (code-auditor) in its own detached, read-only worktree at `7f27262`, briefed with the audit, the frozen corpus and its adjudications, the contracts, the `180278e..7f27262` diff and the commands and evidence. It got no author rationale and no instruction to approve, and it wrote every probe outside the repository. It reported **13 allegations** (10 production, 3 test gaps) plus one minor units note, and edited no tracked file. Every probe was rerun by the author on a clean worktree at `7f27262` and reproduced line for line before any disposition. **All confirmed; none refuted.** Record: `acceptance/M08/remediation/review_first_pass_7f27262.json` (sanitized; the reviewer's probe sources stayed in scratch).

| Allegation | Observed on `7f27262` | Repair (`ea83bc1`) | Regression |
|---|---|---|---|
| R1 High — a pending payload never lapsed | a dropped paste refused job-2 (app B, 1 s later), withheld job-3's copy offer and refused job-4 (2 s later); a newline-normalized paste (field grew) left three later jobs refused and the user's clipboard never restored | readback `normalized` (the field grew by exactly the pasted length, region rewritten) is consumption; a pending payload lapses `PENDING_PAYLOAD_SEC` (5 s) after its post and is restored away while still owned | `test_r01_…` (2) |
| R2 High — a failed final read restored the clipboard | `readback_unavailable`, restored; the late consumer then pasted `CANARY_OLD_CLIPBOARD` | only a destination with no pre-state restores on an unreadable readback; a readable one keeps ownership | `test_r02_…` (with a denied-app restore control) |
| R3 Medium — the selection was not re-checked at the write | a selection made after validation was replaced and reported `confirmed`; an extended strict selection was replaced, then undo reported `undone` with “ beta” lost | the effect boundary re-checks the authorized selection before the AX write, the publication and the post (`selection_changed_before_effect`); a moved caret stays the insertion point | `test_r03_…` (2) |
| R4 Medium — undo armed on `match_ambiguous` | undo deleted “Thanks,” that LocalFlow never wrote | undo is armed only by an attributable `match` | `test_r04_…` |
| R5 Low — caret undo proof compared a zero-length read | `undone` while the text remained | `undone` also needs the field length to move by the owned length less the restored one | `test_r05_…` |
| R6 Medium — terminal category and binding from different reads | multi-line text pasted into the terminal (switch at the second frontmost read) | snapshot-less jobs classify the application validation bound | `test_r06_…` (switch at reads 1–4, editor control) |
| R7 Medium — same-length duplicate re-anchored | the owned range moved onto the user's “cat”; its later edit was recorded as ours (`cap`), the real edit never | a unique candidate must also carry a recorded neighbour (an edge recorded at the field's edge must still be that edge) | `test_r07_…` |
| R8 Low — window title via the frontmost app | the system host asked pid 202 for an element owned by pid 101; capture and validation read a denied app's title (in memory) | `focused_window_title` resolves on `element_pid(el)`; the world models the same rule | `test_r08_…` (the production adapter over stubbed AX calls) |
| R9 Low — deleted job's field read once | one `AXNumberOfCharacters` read after `revoke_job` | a deleted job's pending payload clears before any read | `test_r09_…` |
| R10 Low — deletion mid-transaction re-cached | text and undo record back in the cache; `paste_again` queued | `_remember` checks the revocation under the service lock | `test_r10_…` |
| R11 Medium (test gap) — F06-C05 never held before the write | — | the driver holds after validation on the AX and clipboard paths, with a moved-caret control | corpus driver |
| R12 Medium (test gap) — F16-C04 exercised a different drift | — | multi-line text; snapshot variant plus a snapshot-less job switched at each of its three frontmost reads; editor control | corpus driver |
| R13 Low (test gap) — AUDIT-11 cases passed an observer with no after-text | — | the cases require the observer to stay on its own range and to keep the exact after-text | the reviewer's `bounded_after_always_none` mutant: control 57/57, mutant 56/57 |
| minor — `inserted_chars` units | UTF-16 units on one path | code points on every path | `test_r_inserted_chars_…` |

**Fail-first / pass-final.** `review_regressions_first_pass_7f27262.json`: **43/57**. The 13 review cases fail on the defect each names; so does A1's case (from `10802d0`, after the freeze). The run used an untracked copy byte-identical to `261bfc7`'s suite, and the stamp shows code root `7f27262` with tracked files unmodified. `review_drivers_first_pass_7f27262.json`: F06-C05 fails on both after-validation paths, and F16-C04 fails at live read 2; the pre-validation, snapshot and control variants pass there. On the final code both drivers pass with every declared barrier fired, and the suite passes 57/57 (`regressions_final_9483d1b.json`).

**Author review A1** (before the independent round reported): the pending-payload record was made only with `restore_clipboard` on, so with restoring off a slow target received the next job's payload (`TWO TWO`, ONE lost). Fixed in `10802d0`. `test_a07_pending_payload_kept_with_restore_disabled` fails on `7f27262` and passes after.


### Corpus, stateful, metamorphic and mutation checks

`tests/v2/insertion/m08_corpus_runner.py` materializes every case into the world, drives the real consumers and grades with the adjudication file's label mapping plus the family invariants from the world's logs; installed overrides and declared barriers are recorded (an unfired barrier is a harness error). The corpus file is never edited.

| Code | Cases (125) | Stateful (18) | Metamorphic (9) | Record |
|---|---|---|---|---|
| base `180278e` | 56 pass · 55 fail · 14 not run | 14 pass · 4 fail | 6 pass · 3 fail | `corpus_base_180278e.json` |
| first pass `82de7df` (production `846fd2c`) | 116 pass · 9 not run | 18 pass | 9 pass | `corpus_first_pass_82de7df.json` |
| final `9483d1b` (production `ea83bc1`) | **116 pass · 9 not run** | **18 pass** | **9 pass** | `corpus_final_9483d1b.json` |

All 25 families pass on the final code. The 9 not-run cases are the native-helper cases (F07-C01..C04, F09-C04/C05, F10-C05, F11-C02, F12-C04), never passed by the portable runner (D17) and graded by the owned-native suite below. F25 (benchmark cases) grades the final benchmark record.

**Mutation check** (`scripts/v2/m08_mutation_check.py`: disposable `git archive` copies, each patch proven applied and each killing run proven to import from the copy; a red control, an unfired barrier or an error is never a kill): **20/20 killed** at the first pass (`82de7df`) and at the final tool head (`75fcb80`, `mutation_final_75fcb80.json`). At `9483d1b` MUT-08 and MUT-12 reported “mutation not applied” — their anchors were code the review round rewrote — and were counted as harness errors, not kills; `75fcb80` re-anchors both on the final code with the same weakening. MUT-18's primary killer is native (F07-C01): killed natively on the final code (`mutation_mut18_native_final_9483d1b.json`, owned `(4, 7)` in mixed units); the portable F07-C05 kills it too. One weak oracle was found this way and fixed (`221c172`): the native astral case accepted `posted_unverified` without checking its owned range, so MUT-18 survived natively with a mixed-unit `(4, 7)`. The reviewer's own mutant (`bounded_after_always_none`) survived the regression suite at `7f27262` and is killed by it after `261bfc7`. Benchmark mutants BMUT-01..06: 6/6 rejected (work invalid, exit 2) at the first pass and at `9483d1b` (`benchmark_mutation_final_9483d1b.json`).

### Verification — portable, shim and native (kept distinct)

- **Portable, real-framework over synthetic worlds** (isolated; real AppKit/PyObjC imports, the world or fixture as host): `run_isolated.py tests/v2/insertion/test_m08_remediation.py` 57/57; the historical `test_insertion_races.py` 13, `test_insertion_attribution.py` 10, `test_insertion_pipeline.py` 5; `test_selection_capture_m06_seam.py` 8; `test_transform_remediation.py` 37; `test_transform_remediation_native.py` 16 (despite its name, it drives the fixture target through real AppKit — portable evidence, not an AX write to an external process). Full sweep of every tracked test file except the five model-backed ones: `sweep_final_9483d1b.json` — 88 files, 86 exit 0, 985 ok lines, zero live-clipboard changes, zero blocked desktop calls; `test_native_ax.py` and `test_native_insertion.py` exit 2 (NOT RUN) under isolation by design. No pre-existing failure appeared; no M12/M14 harness race was encountered.
- **Owned-native** (natively, keyboard untouched, each helper reached by pid): `tests/v2/insertion/test_native_insertion.py` over `native_insertion_target.py` (real `AXUIElement`, `AXValue`, `NSTextView`, `NSPasteboard`; a private named pasteboard): **12/12 counted** (F07-C01..C04 native ranges and undo incl. astral/combining/ZWJ, F12-C04, F09-C04/C05 publish and restore refusals, F11-C02, four binding checks), 1 residual characterized (F10-C05: a copy landing between the generation check and the native restore write is overwritten — the documented non-atomic residual), 5 diagnostic (`native_suite_final_9483d1b.json`). The M06 `tests/v2/context/test_native_ax.py`: 7/7, including the M08 native bridge.
- **Model-backed control:** `tests/v2/training/test_live_pipeline.py` 2/2 (`live_pipeline_final_9483d1b.json`; synthetic audio; M08's behavior needs no model).
- **Manual:** the eight runbook checks below; no native helper pass is recorded as a manual one.

### Reference-Mac performance (`docs/v2/benchmarks/20260926-074103-m08-remediation-final/`)

Work-valid (`work_valid: true`, 0 invalid samples; every sample carries its own witness: bound target and owner, method, expected bytes and effect count, required reads, clipboard generation decisions, the queue thread, the operation id). Canonical budget S24 (UI acknowledgment p95 ≤ 50 ms): **pass** for every UI entry point — `_finishWithText_` 0.010 ms, `pasteLastResultAgain_` 0.104 ms, `hubPasteText` 0.111 ms, `undoLastInsertion_` 0.045 ms p95 (n = 20 each). Proposed PROPOSED-M08-B1 (native validation → AX write → readback p95 ≤ 150 ms, not a specification figure): **pass** — warm 1.111 ms (n = 29, all confirmed, 10 astral), cold 42.6 ms (n = 6). Queue wait, worker execution, settle, observer tick/re-anchor/stop, undo and repaste are reported separately with the clock each injected delay belongs to (README). The undo call-to-effect clock rose by one injected 200 ms delay — the pre-write length read behind the `undone` proof; the callback clock is unchanged. First-pass run (production `846fd2c`, AC power): `benchmarks/20260926-062315-m08-remediation/`.

### Contracts changed (and why)

`contracts/insertion.md` states the repaired design (destination binding and the effect boundary, including the authorized selection; separate read capability; strict/plain replacement proof; UTF-16 host units; attributable confirmation and the `normalized` readback; the pending payload, its lifetime and ownership before the post; transaction truth; operation identity; target-bound undo armed only by an attributable readback, with its length proof, and the recovery lifetime; queued Paste Again; observation admission, per-tick authority, unique re-anchoring with neighbour provenance, bounded edits and exactly-once close; deletion revocation), the local caller inventory and the `m08-policy-v1` decisions. Interface addenda: `targets.md` (the bound element), `context.md` (D5 at validation time; field elements never persisted), `training_evidence.md` (capture-time consent for the observation producer). No frozen identifier changed; no schema change (the `insertions`/`insertion_observations` columns are unchanged — units and the after-region live in existing JSON fields); no ADR.

### Historical records (2026-09-22)

The September 22 M08 results, benchmark (`benchmarks/20260922-131238-m08/`) and pending item `native-m08-insertion-trial` are preserved unchanged in `results.json` and above; the `remediation_2026_09_26` block there and this addendum supersede only their claims about the current code. The pending item is carried by M08-V001–V005 under instruction revision M08-r1.

### Pending manual verification (`docs/v2/VERIFICATION.html` → M08, instructions M08-r1, code `ea83bc1`)

All eight start Not run; the frontend-design skill was invoked before the section was written, and with the M08 section and the last-updated line masked the page is byte-identical to `180278e`'s (script, storage key, navigation and every other milestone's checks unchanged).

- **M08-V001** real destination classes, and an app switch right after releasing fn (cross-reference M06-V003) — records which destinations confirm.
- **M08-V002** copy styled text or an image while a dictation is processing: the copy survives and the clipboard comes back.
- **M08-V003** undo after a manual edit and with a look-alike document in front; nothing newer lost (cross-reference M11-V003).
- **M08-V004** two rapid dictations land once each, in order; Paste Again never doubles text.
- **M08-V005** a harmless line and a two-line command into Terminal: nothing ever runs; no surface is certified.
- **M08-V006** a denied app, a password box and a transform accept into another app (cross-reference M11-V001/V002; not re-graded).
- **M08-V007** observation in real apps: a corrected word, typing elsewhere and a document switch; only the dictated words are watched.
- **M08-V008** Recovery/History stays responsive; after deleting a dictation nothing of it can be pasted or undone (sandbox store).

### Limits

- The effect-boundary checks and the effects are not atomic: a focus change, a new selection or a copy landing between them is the documented residual (microseconds).
- The pasteboard generation check and the native restore write are not atomic (native F10-C05 characterization).
- A consumer later than the settle bound on a surface that may not be read pastes after restore; on readable surfaces ownership lasts `PENDING_PAYLOAD_SEC` (5 s), after which a dropped paste is abandoned and the user's clipboard restored while still ours.
- The paste post goes to the key focus; the bound check before it narrows, but cannot close, a switch.
- `CERTIFIED_BRACKETED_SURFACES` is empty: every terminal insert a terminal could act on is copy-only.
- Paste Again's substring reconciliation is approximate by design (D16).
- Native qualification covers the AX path and pasteboard ownership on owned helpers; real applications and the ⌘V path into real destinations are the manual checks.

### Privacy

Synthetic worlds, synthetic helper windows, private named pasteboards and temporary stores only; no installed app, real destination, real clipboard content, transcript, audio or database was read or committed. Committed evidence carries ids, counts, statuses, timings, hashes and synthetic strings; a scan of every evidence file and the branch diff found no user paths. The reviewer's probe sources stayed in scratch.

### Orchestration overview

Realigned in place on this branch (no layout change; checked at 390 px with no text overflow, and in both appearances), against this addendum and the `results.json` block, not session memory:
- **NOW verdict and campaign row:** next is the campaign's next read-only audit, once this branch merges; M01–M08 and M11 are audited and remediated.
- **M08 strip:** state, benchmark and human-verification rows updated, plus a new Remediation row.
- **Should-run order:** M08 done, on the branch. The graph permits the next audit now, but it should run on `main` after the merge. M09, M10, M12, M13 and M14 remain unaudited, and the repo records no order among them.
- **Runbook ledger:** the native-m08-insertion-trial row points to M08-V001–V005.
- **Open threads:** “M08 read-only deep audit — next” became “M08 remediation — merge, then manual verification”, and the insertion-trial row names its runbook checks.
- **Stamp and figure-audit comment:** updated.

No other session held the page: it was clean in the only checkout, with no other worktree on it.


### Merge readiness

**Recommendation: merge.** The branch is a fast-forward of `main` (`origin/main` is still `180278e`). Every automatable, native-local, benchmark and independent-review requirement ran and is green on the committed final tree:
- regressions 57/57;
- corpus 116 pass / 9 native-only, stateful 18/18, metamorphic 9/9;
- owned-native 12/12, with 1 residual characterized; M06 native 7/7;
- mutants 20/20 and benchmark mutants 6/6;
- sweep 86/88, the other two being the native suites NOT RUN under isolation by design;
- model-backed 2/2.

All 13 review allegations were reproduced, fixed and pinned by fail-first regressions.

**What merging does not certify:** real applications. M08-V001–V008 are Not run, and no native helper pass stands in for them.

**Behaviour changes a user will notice, all deliberate** (safety over convenience):
- A dictation is saved to History instead of typed when the destination or its selection changed after validation.
- A clipboard-path dictation within 5 s of a paste that has not landed is saved rather than published over it.
- Undo declines when the readback could not attribute the text.
- Nothing is confirmed or observed in a denied app or a password field.


### Merge — 2026-09-26

The branch was fast-forwarded onto `main` at `145488c` (no merge commit: `main` had not moved from `180278e`) and `main` was pushed. Final production `ea83bc1` is unchanged by the merge. M08-V001–V008 remain Not run and are now run against `main`.
