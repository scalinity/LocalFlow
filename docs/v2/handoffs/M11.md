# M11 Handoff — Transforms and the requirement-preserving Prompt Engineer

**Milestone:** M11 · **Requirements:** LF-R17, LF-R18, LF-R28, LF-R30, LF-R31 · **State:** `implemented_pending_human`
**Base commit:** `6e52328` (branch `main`, remote `scalinity/LocalFlow`) — the M10 close-out ORCHESTRATION realignment; the M11 commit follows this handoff.
**Tree at start:** clean; no other worktrees; no other session owned any file M11 touched.
**Tree at end:** M11 adds `localflow/v2/transforms/` (`__init__, prompts, definitions, atoms, engine, diffview`), `localflow/v2/transforms_store.py`, `localflow/v2/ui/transforms_panel.py`, `tests/v2/transforms/` (fixtures_prompt_engineer.json — 30 cases, 4 suites, 30 tests), `scripts/v2/benchmark_m11.py`, `docs/v2/{contracts/transforms.md, acceptance/M11/, benchmarks/20260923-013025-m11/}`; modifies `localflow/{app,overlay}.py`, `localflow/v2/{store,worker,supervisor,profiles,training,history_queries}.py`, `localflow/v2/ui/{state,hub}.py`, `tests/v2/lifecycle/fake_worker.py` (transform op), `tests/v2/profiles/test_style_resolution.py`, `tests/v2/ui/{test_history_queries,test_hub_shell}.py`, and `docs/v2/{STATUS.json, contracts/{INDEX,store,hub,profiles,training_evidence,preferences}.md}`. The capture journal, worker protocol framing, M07 cleanup engine, M08 insertion queue semantics, V1 cleanup control, installed app and all M01–M10 artifacts are otherwise untouched (no build authorized).

## What shipped

1. **Versioned definitions with preserved revisions (S16, AC01).** `transforms.definitions` + `transforms_store` (schema v7): built-in polish/concise/prompt_engineer contracts (content-hash prompt revisions), custom transforms with declared edit scope, optional writing samples, single-char shortcuts (collision-checked at write time; the legacy keys 1/2 recorded but never bound), target profiles and the opt-in auto-apply flag. Every edit appends to `transform_revisions` — delete disables, nothing is ever erased; the two uploaded V1 definitions materialize from the M02 legacy artifacts frozen (prompt verbatim, edits refused).
2. **The execution engine with task identity (S16/S29.10, AC05).** `TransformJob` freezes source/transform/prompt/instructions/examples; `task_key` = hash(mode, source, instructions, examples revision, prompt revision, locale). Retry-original is the same task; transform-of-result and any changed source/instructions/prompt revision are different tasks — the store refuses cross-task judgments at write time. One bounded greedy generation on the cleanup engine's loaded model in the worker (`transform` op; GPU-serialized with dictations). Paths: `applied` / `needs_review` (original clauses kept, never auto-applied) / `fallback_original` (failure, output limit, refusal — an oversized source refuses at 12,000 chars).
3. **The Prompt Engineer's requirement-atom map (S16, AC02).** `atoms.py` extracts atoms deterministically (questions, negations, edit constraints, counts, ordering, deadlines, uncertainty, quoted spans, URLs/paths) and reports three-valued coverage per atom with the output span carrying it; missing/uncertain atoms are review issues surfaced beside the diff — never silently dropped, and auto-apply declines. Every mode guards quoted spans/links/identifiers verbatim. The uploaded prompt's "relevant context"/"format if implied"/"be concise" wording appears nowhere.
4. **Selected-text execution (S16, AC03).** Status menu → Transforms (rebuilt on open) → capture (selection + snapshot-shaped target identity, off the UI callback) → generation thread → the preview panel: block diff (+ word diff for short sources), REVIEW clauses kept, Accept / Copy / Retry Original / Apply Another… / Transform Output… and an honestly-labeled Save-to-Scratchpad (M12). Accept routes through the M08 insertion queue with revalidation — a changed selection never overwrites (target_changed → copy offer). The panel is non-activating; undo of an accept is the M08 target-bound undo. The pill acknowledges immediately (MODE_TRANSFORMING).
5. **Mode executors + opt-in auto-apply (S15, AC04).** All six modes are executable; a transform-backed mode executes through its bound definition when the definition opted in AND targets the resolved profile — otherwise Clean with the honest reason (`transform_auto_apply_disabled` / `transform_profile_not_targeted` / `transform_not_bound`), visible in the menu line, the envelope and (new) the Styles rule editor offering all six modes. The dictation path transforms the **Clean artifact** after it is recorded and before finalize: Clean stays the cleanup stage's applied output, the transform writes its own stage artifacts, and only a validated result changes the inserted text. Cleanup-off honestly refuses (`transform_requires_cleanup`).
6. **Evidence + Hub (S29.4, S19).** `on_transform_result` retains the exact prompt/output lease-governed and fills the content-free envelope `transform` block; `note_transform_gate` carries requested-but-not-run reasons; History's fourth lineage stage resolves from `transform_output` artifacts. Store v7 adds `transform_candidates`/`preference_observations` (rows id/hash-only; texts in artifacts inside the same writer op; display order ranks candidates within a task). The Hub Transforms view (VIEWS 7→8) rides the `_build_/_refresh_/_load_` triple with `transforms_service`; Scratchpad/Insights still refuse at state level.

## Contracts and decisions

- New `contracts/transforms.md`; `contracts/INDEX.md` row + decision note; `store.md` v7 section; `hub.md` (8-view list, transforms_service, lineage note); `profiles.md` (all-modes-executable resolution + the opt-in gate reasons); `training_evidence.md` M11 live status; `preferences.md` M11 live status. No frozen identifier changed; registry stays 33/22; no ADRs — no spec deviation accepted.
- **Auto-apply settled as the definition-level opt-in (AC04):** the mode (override/rule/category) is the request; the definition's `auto_apply` + target profiles decide execution. Built-ins ship auto-apply OFF — zero configuration equals exact M03–M10 behavior (pinned by test).
- **The atom map is deterministic, three-valued, and review-degrading:** uncertain/missing never auto-apply and the original clause is displayed — the S16 "keep the clause or mark a review issue" rule implemented as the review path.
- **Task identity includes the prompt revision:** an edited contract never pairs candidates across the change (AC05 hardened beyond the S16 minimum).
- **The panel delegates undo to the M08 engine** (Recovery menu) rather than building a second undo path.

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M11/results.json`. All cwd = repo root, all exit 0:

- **Pre-change baselines:** 36 fast suites + 6 model-backed suites green (incl. the V1 control 51/51, 48.9 s).
- **Post-implementation and post-address:** definitions **8/8**, engine **8/8**, pipeline **10/10** (real coordinator + real InsertionService over the fixture target), PE cases **30/30** model-backed (28 applied + 2 honestly flagged, zero silent losses, 58.0 s inference); all 39 fast suites; all six model-backed checks; registry 33/22; privacy scan clean.
- **Benchmark** (`20260923-013025-m11/m11.json`): 504-word transform p95 **0.708 s** (budget 15); UI acknowledgment warm max 0.026 ms + snapshot rebuild 0.069 ms (budget 50); dictation delay behind an in-flight transform **0.649 s** (real worker, serialized GPU lock — recorded).
- **Live store migrated 6 → 7** with a pre-migration backup; verify() clean; built-ins seeded; both legacy definitions materialized; jobs/legacy rows untouched.

## /review-2 → /address record

Two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 2 criticals (both corroborated), 12 warnings, ~13 suggestions after dedup.

Criticals: (1) **both reviewers** — the preview panel was constructed with a direct PyObjC class call (`TransformPreviewPanel(self)` → TypeError), so the entire review/accept surface could never open while every suite stayed green (the tests called `tfAcceptTransform` directly — the M10 C1 class one layer up); (2) **both reviewers** — the transform work threads were unguarded: an oversized selection raised at job construction and a `WorkerFailure` on the retry path escaped `_m11_run_job`, either wedging `_tf_active` permanently (transforms lockout + stuck pill until restart).

`/address` fixed both in-session with regression tests (panel constructs through `_tfShowResult_` via `alloc().init_panel`; `_m11_run_transform` is total with the None-job refusal handled end-to-end, `_m11_run_job` guards the request, and every `work()` runs through a guarded `_tf_spawn` whose failure path still reaches `_tfShowResult_`; a 20k-char selection refuses with no wedge) and every warning: the shared busy guard on all panel actions; the non-activating panel (accept-time revalidation no longer races its own activation); the (location, length) tuple convention; Transform Output… wired as the distinct transform-of-result command; the total dictation executor (a transform fault can never skip finalize/history for Clean); the worker's job-inside-try refusal channel; the v7 repair set; per-row legacy materialization (one malformed row can't disable the service); id-AND-mode built-in binding with the store refusing mode changes; `_tf_results` removed with per-task display order; the gate reason threaded to the envelope; the honest UI-acknowledgment benchmark. Suggestions applied: anchors in `Coverage.to_json`, prompt revision in the task key, revision read inside the writer op + no-op updates appending nothing, sentence-span offsets, word-boundary single-word anchors, `Optional` annotation, the id-only dictation-candidate comment, the inline word diff. **No findings rejected as incorrect; none deferred.** Post-address, all 39 fast suites, the 30 PE cases, all six model-backed checks and the benchmark re-ran green on the fixed code. (The /address per-fix-commit cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's explicit constraints.)

## Human verification (pending; steps in results.json)

`native-m11-transform-trial`: from Terminal.app — a real draft through Prompt Engineer (pill stage, non-focus-stealing panel, per-atom diff inspection, REVIEW items with original wording), accept + target-bound undo, Retry Original / Apply Another / Transform Output, a custom transform with a writing sample + auto-apply under a custom-mode rule (Clean retained in History's cleaned stage), and the legacy rows shown as preserved revisions with screenshots at normal and enlarged text scale. The ten earlier pending trials (M10 developer, M09 hub, M08 insertion, M07 long-prompt, M06 destination, M05 dictionary, M03/M04 native items, M01 release-to-insert pilot) stand in their own results files; none gates M12's automatable work.

## Limitations / unresolved

- Documented in `contracts/transforms.md`: the atom extractor is deterministic and conservative (English anchors); uncertain coverage is honest hedging; selection capture needs AX trust (denied apps refuse); the serialized GPU lock delays a concurrent dictation's ASR behind a transform (measured 0.649 s — recorded); the panel's visual pass is the pending trial.
- The installed /Applications app still runs pre-M02 code (no build authorized); M11 exists in dev runs and tests.
- Unknown from M01–M10, unchanged: producer source of stats/dictionary/transforms; `instance.lock` producer; the M01 release-to-insert pilot (M15 gate).

## Privacy

Nothing private entered Git: fixtures are synthetic (origin-tagged in-file; docs.example.com / src/pipeline/stage3.py); the envelope's transform block and every event carry ids/revisions/counts/reason codes only; exact prompts, sources and outputs live exclusively in lease-governed store artifacts under the training-buffer lease (the IPC-only prompt is annotated as such); preference rows are id/hash-only; the benchmark JSONs carry latencies/counts; the live v7 migration is additive with a backup under `v2-evidence/backups/`; live data stays under Application Support; the baseline-manifest privacy scan passes over every new path.

## Reusable patterns for M12+

- **Total request helpers:** `_m11_run_transform`/`_m11_run_job` never raise — every failure returns an honest `fallback_original` result, and thread bodies run through a guarded spawner whose failure path still clears the in-flight marker. The wedge class (a cleared-only-on-success flag) is closed structurally.
- **Construct NSObject surfaces the way production does, and test that path:** the review-C1 lesson now has a second worked example — the pipeline test constructs the panel through `_tfShowResult_` (`alloc().init_panel`), not by calling the coordinator action directly.
- **Gate reasons are first-class:** every "requested but not run" state records a specific reason that reaches the menu line AND the envelope (`note_transform_gate`) — the honest-fallback idiom extends from modes to stages.
- **Task identity for preferences:** hash every input that defines the task (mode, source, instructions, examples revision, prompt revision); enforce same-task at write time; record display order within the task.

## M12 eligibility

**Dependency-eligible: yes.** M12 (Scratchpad and local writing versions) depends on M09, M11 — both delivered. M11's preview panel already carries the honestly-disabled Save-to-Scratchpad control M12 activates, and the transform engine's versioned-artifact pattern is the natural base for writing versions. Pending human checks (M11's trial and the earlier ones) do not gate M12's automatable work.
