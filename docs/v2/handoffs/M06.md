# M06 Handoff — Local context snapshots and destination awareness

**Milestone:** M06 · **Requirements:** LF-R09, LF-R28, LF-R29, LF-R30 · **State:** `implemented_pending_human`
**Base commit:** `e9874c62ee21d6b00d2760a991798e637ba671cf` (branch `main`, remote `scalinity/LocalFlow`)
**Tree at start:** clean (ORCHESTRATION.html committed at e9874c6 by a dedicated non-milestone session; untouched during the milestone — realigned at close-out, see below). No other worktrees; no other session owned any file M06 touched.
**Tree at end:** M06 adds `localflow/v2/context/` (`snapshot.py`, `providers.py`, `collector.py`, `__init__.py`), `tests/v2/context/` (2 suites + `fixtures_context_targets.json`: 32 E10 scenarios + 9 adversarial trees), `scripts/v2/benchmark_m06.py`, `docs/v2/{contracts/context.md, acceptance/M06/, benchmarks/20260922-115621-m06/}`; modifies `localflow/{app,config}.py`, `localflow/v2/{training,capabilities}.py`, `tests/v2/fixtures/manifest.json`, `docs/v2/{STATUS.json, contracts/{INDEX,targets,vocabulary,asr_hints,normalization,training_evidence}.md}`. The worker protocol, capture journal, store schema (stays v3), normalization/vocabulary domains, cleanup adapter, installed app and all M01–M05 artifacts are otherwise untouched (no build authorized).

## What shipped

1. **The context package (Spec S12, EV-08).** `localflow.v2.context`: `TargetSnapshot` (cheap frontmost identity via NSWorkspace — bundle/pid/name, denied decision, category; NO Accessibility on the hotkey path), `ContextSnapshot` (identity + field/selected/nearby text + origin + workspace + identifiers + provenance + freshness + per-provider status/duration + omission reasons; immutable; `to_json` for artifacts, `to_envelope_block` content-free), providers behind an injectable `SystemAXHost` (messaging-timeout-bounded AX reads; one cached systemwide element) and `frontmost` accessor, and `ContextCollector` (identity → async collection during recording → bounded finalize at release, deadline `context_deadline_ms` default 75; partial on cut; per-job collection handles; downstream revisions; short-lived origin/workspace cache invalidated on app/focus/window change).
2. **Sensitive-field absolutes (S12).** Classify-before-read (role/subrole decide `secure|text|unclassifiable|none` before any content attribute); secure fields retain no content anywhere; unclassifiable fields get plain dictation without nearby text; denied apps are never read at all (including their origin/window title — review-fixed); browser origins strip query/fragment (`AXURL` source, marked window-title fallback with a plausible-host check, honest `not_exposed` otherwise); placeholders are metadata never text; no filesystem reads (document locators are recorded strings; only `file://`/absolute paths yield workspaces); nearby reads bounded (600 chars) and identifiers capped (64).
3. **One snapshot, three consumers (the settled wiring).** The finalized `ContextSnapshot` feeds: the M05 `ScopeContext` (app/site/workspace — profile stays None until M10), the M04 engine `ContextSnapshot(destination_app, path_context, identifiers, vocabulary)` (the job's frozen vocabulary rides through), and the S30.1 `context_snapshot_id` in `asr_hint_request_fields` (called on the real dictation path now; None under the unqualified adapter).
4. **Pipeline wiring (app.py).** Identity captured after the overlay, before the M05 trio (trio scoped by the app identity); collection runs during recording; finalize + **scope upgrade** at release, before `job_started`/`on_hint_set` — when the resolved origin/workspace widen the scope, the trio rebuilds from the job's **frozen entry set** (no store read; M05 AC03 preserved live) and the upgraded hint set is what is stored pre-decode. Late provider results are picked up by the coordinator as a separately-identified downstream revision via the job's own collection handle. Every context call is guarded — a context failure degrades to less context with an event, never a dropped dictation.
5. **Evidence (S29.4 context family; S29.14).** `EvidenceCollector.on_context_snapshot` writes the full snapshot JSON as a lease-governed artifact (`pre_decode_context`/`downstream_context`) and fills the envelope's content-free `context.destination`/`context.downstream` blocks; the absent halves carry honest reasons. Retention is the independent `training_retain_context` knob: off ⇒ no payload (pre-decode and downstream), `retained: false` + reason + `missing_reasons.context_snapshot_payload` marking replay inputs incomplete.
6. **Config (S25-visible).** `context_enabled` (default true), `context_deadline_ms` (75), `context_denied_apps` ([]), `training_retain_context` (true).
7. **Diagnostics.** Content-free events (`context.snapshot_finalized` with coverage counts/durations, `context.cache_invalidated`, `context.identity_unavailable`, `context.capture_failed`, `context.finalize_failed`, `context.collector_unavailable`, `context.provider_failed`, `context.destination_changed`); plain dictation remains available in every degraded mode (tested through the real coordinator).

## Contracts and decisions

- New `contracts/context.md` (capture structure, one-snapshot-three-consumers, sensitive-field absolutes, freshness/caching, degradation, evidence retention, hosts/testability, limitations). `contracts/INDEX.md` row + decision note; `targets.md` snapshot half live; `vocabulary.md` M06 wiring + limitation update; `asr_hints.md` M06 live status; `normalization.md` dependency live; `training_evidence.md` M06 live status. No frozen identifier changed; registry stays 33/22; no ADRs — no spec deviation accepted.
- **Where the S12 bounded snapshot is captured (settled, per the kickoff):** identity at PTT-start (post-overlay, NSWorkspace only); async collection during recording; bounded finalize (≤75 ms) at release in `_finishCapture`, **before** `job_started`/`on_hint_set`/enqueue — everything context-related is pre-decode by construction, ASR starts strictly after, and late providers surface only as downstream revisions. Reason: the spec's own structure ("inexpensive target identity at PTT start; collect asynchronously while recording; finalize a bounded snapshot at release", "initial post-release deadline is 75 ms") and M05's freeze semantics (the hint set must be scoped by the destination before decoding; the evidence write already sits after the finalize point).
- **Partial degradation:** timed-out providers are omitted with reason `deadline` (`provider_failed` for exceptions, `destination_changed` for frontmost drift, `permission_unavailable`/`ax_messaging_failed` for AX states); `partial` is true for genuine degradations, not for a legitimate `not_exposed`.
- **Scope upgrade from frozen entries (not the store)** — the AC03-preserving rebuild; a mid-flight dictionary edit changes only future jobs (tested live).
- **Jobs table carries no `target_snapshot_id` column** (settled): the ids live on the job dict and in the evidence context block; M08 adds what insertion validation needs at its own migration. No speculative schema.
- **Window-title origin fallback** is provenance-marked heuristic with a plausible-host check; browsers that expose no AXURL report no origin rather than a guess.
- **`profile` scope stays unfed live** — no writing-profile subsystem exists before M10; profile-scoped entries apply only in tests/the sandbox.

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M06/results.json`. All cwd = repo root, all exit 0:

- **Pre-change baselines:** 4× M01 suites, event writer, store, import, collector, 4× M03 lifecycle suites, 4× M04 normalization suites, 4× M05 vocabulary suites, stuck overlay (all pass); worker_live 2/2 (model-backed), live pipeline 2/2 (model-backed), cleanup 51/51 (model-backed, 45.7 s).
- **Post-implementation and post-review:** the two new M06 suites — `tests/v2/context/test_context_snapshot.py` (19 tests pre-review → **27 post-review**: the 8-class identity/scope matrix, AC01 secure-field zero-read/no-retention, classify-before-read, unclassifiable, permission-unavailable, denied app + **denied browser**, no-focused, AC02 deadline cut with explicit slack + one-shot downstream, idempotent finalize, AC03 stale invalidation + cache reuse + same-app window-change sentinel, AC04 hostile-text-as-data, mid-sentence, placeholder, selected ranges, origin lifecycle, title-fallback host plausibility, identifiers, ScopeContext adapter, context-disabled, **stale/abandoned finalize refusal**, **provider-exception honesty**, **destination-drift zero-reads**, window-None, pid-less fallback) and `tests/v2/context/test_context_pipeline.py` (12 tests: live workspace/site scope feeding vocabulary, scoped hint set frozen pre-decode with destination evidence + content-free envelope, AC05 late-context downstream (pre-decode artifact never grows), post-answer no-leak, context-disabled with honest absent-destination reason, retention reconstructable-vs-redacted incl. downstream, AC04 cleanup-input identical, placeholder never prepended, AC03 frozen-entry upgrade, S30.1 request fields carry the snapshot id, PTT-usable-without-AX, secure-field pipeline) — plus every pre-change suite re-run green, worker_live 2/2, live pipeline 2/2, cleanup 51/51 (47.9 s).
- **Benchmark** (`docs/v2/benchmarks/20260922-115621-m06/m06.json`, post-review code, no model call): identity capture p95 **0.008 ms** (post-overlay; hotkey→overlay untouched); finalize p95 **0.303 ms** vs the 75 ms budget; worst-case provider cut **80.4 ms** (deadline + scheduling slack; the cut provider is delivered downstream); provider coverage **52/96 slots resolved** across the 32-scenario matrix with per-provider counts and skip rate 0.458 (honest per-class coverage, not just successful-read latency); scope-upgrade cold rebuild at 10,000 entries **62.2 ms** (cached per (revision, scope) thereafter); paired pipeline delta **0.42 ms**.

## /review-2 → /address record

`/review-2` ran two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 2 criticals, 7 warnings, 11 suggestions after dedup. The criticals: (1) **corroborated by both reviewers** — a denied app that is a browser was still read by the site-origin provider and its origin persisted into retained artifacts, violating the S12 denied-app absolute (the existing test used an unknown-category app and missed the leaking class); (2) collector state was global, not per-job — a failed identity capture could finalize the previous dictation's snapshot into the new job's evidence, and `take_downstream` could attribute a newer dictation's late context to the job still processing.

`/address` fixed both criticals and every warning in-session with regression tests: denied gates end-to-end (provider params + composition hard-null + denied-browser fixture/test with zero-read and no-leak assertions); per-job collection handles (begin returns the handle, finalize refuses stale/foreign targets, abandon on failed identity, one-shot downstream from the job's own handle); honest absent-destination envelope marker; provider exceptions recorded as `provider_failed` + event (never a fake deadline) and downstream rows marked `not_in_revision`; locals-first scope upgrade; `is_path_document` scheme gating (http URLs never yield workspaces/path_context); plausible-host title fallback with an extension denylist; destination-drift check before content reads; and all flagged test gaps closed. Ten suggestions applied (dead-symbol removal, `_PARTIAL_REASONS` from constants, generator→plain collection method, cached systemwide AX element, tolerant `from_json`, real call site for `asr_hint_request_fields`, cache locking, isinstance guards, comment alignment — see results.json `review_loop.addressed`). One deferred with its reason: a seventh envelope missing-reason value for the retention knob — the six-value vocabulary is frozen in `contracts/artifacts.md` and the precise `training_context_retention_disabled` reason already lives in the destination block; changing frozen vocabulary needs its own contract decision. **No findings rejected as incorrect.** Post-address, every suite and both model-backed checks re-ran green and the benchmark was re-run on the fixed code. (The /address per-fix-commit cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's explicit constraints.)

## Human verification (pending; steps in results.json)

`native-m06-destination-trial`: dev-run from Terminal.app (which needs its own Accessibility grant — ZCode-hosted terminals get `kAXErrorCannotComplete`, the AX analogue of the mic-TCC gotcha; one instance at a time). Dictate the same phrase across Gmail, Slack, an AI prompt field and Xcode; verify mid-sentence and selected-text snapshots; secure-field and denied-app behavior; record steps/expected/actual. The M05 dictionary trial's workspace-scoped live half is now runnable as part of this trial (destination awareness shipped); the other M01/M03/M04/M05 native items stay recorded in their results files; none gate M07.

## Limitations / unresolved

- Documented in `contracts/context.md`: profile scope waits for M10; identifier resolution is the bounded nearby window (a full visible-symbol provider needs the optional IDE/browser extension channel — not shipped); the title-origin fallback is heuristic and provenance-marked; no insertion revalidation yet (M08 owns the `same_destination` consumer); jobs table has no target column by decision.
- The installed /Applications app still runs pre-M02 code (no build authorized); M06 exists in dev runs and tests.
- Unknown from M01–M05, unchanged: producer source of stats/dictionary/transforms; `instance.lock` producer; the M01 release-to-insert pilot (M15 gate).

## Privacy

Nothing private entered Git: committed fixtures are synthetic (origin-tagged, `com.example.*` values only); the envelope's context blocks carry ids/flags/counts/reasons only — bundle ids, origins, workspaces, window titles and field text live exclusively in lease-governed store artifacts under the independent retention knob; denied apps and secure fields retain no content anywhere (canary-tested across snapshot JSON, envelope and events); context events are content-free by construction; the baseline-manifest privacy scan passes over every new path; live data stays under Application Support / Logs, outside the repository.

## M07 eligibility

**Dependency-eligible: yes.** M06 delivered the destination-aware snapshots M07's faithful cleanup consumes (permitted context becomes definable against a real snapshot; the hostile-text discipline and the "cleanup input is exactly the transcript" invariant are already tested). M07 depends on M03/M04/M05/M06 — all delivered. Pending human checks (the M06 destination trial; M05 dictionary trial; M03/M04/M01 native items) do not gate M07's automatable work.

## Remediation addendum — 2026-09-26 (local session on the reference Mac, post-audit)

**Input:** read-only audit “LocalFlow V2 — M06 Read-Only Deep Audit” at `6a083bd70011988314836164e7d3016da3968490` (verdict C; 25 findings: 1 Critical, 8 High, 7 Medium, 5 test gaps, 4 design concerns; report sha256 `53b12191…5611`) and its adversarial corpus (101 cases / 50 families, 10 metamorphic relations, 18 mutation definitions, all `NOT_RUN`; sha256 `e2a2b561…9c6`, committed byte-identical as `tests/v2/context/m06_audit_corpus.json`). The audit executed nothing. **Synchronization:** local `main` was `1ee8e449ad21382378785c7ca9c309f55020d44f` (0 ahead / 31 behind, no local-only commits); remote `main` at synchronization was `6a083bd` — unchanged since the audit; fast-forward only, local `main` after = `6a083bd`. The accepted M01–M05 remediation commits were verified as ancestors. Two untracked paths from a separate M11 session (`docs/v2/benchmarks/20260924-m11-remediation/`, `tests/v2/transforms/test_transform_remediation.py`) were left untouched and never staged. **Branch:** `m06-local-remediation`, created at `6a083bd`; not merged.

**Commits:** `281733e` base reproduction, corpus runner, fail-first regressions, base evidence (no production change) · `88ee303` **first-pass production repair (frozen for review)** · `545e21b` first-pass tools (work-valid benchmark, mutation check, native suite) · `9435846` evidence-tool fixes found on the first-pass run · `4388b52` independent-review fixes — **final production code** · `5f277d7` review-round regressions and tightened tools · `fd91032` contracts · `a41dfcc` repro stamp ignores untracked files · `3a2d8e1` MUT-14 re-anchored on the final guard — **final test/tool head**; every later commit is evidence and documentation only.

**Environment:** reference Mac — Apple M5 Pro, Mac17,8, 18 CPUs, 48 GiB, macOS 27.0 (26A428), arm64; `.venv` CPython 3.14.7; PyObjC 12.2.1 (AppKit, ApplicationServices, Quartz); MLX 0.31.2 on Metal; Accessibility trusted for the terminal; models from the local Hugging Face cache (Parakeet TDT 0.6B v3 snapshot `ed2b7e8c…`, Qwen3-4B-Instruct-2507-4bit snapshot `50d42775…`), offline. **State:** `LOCAL_REMEDIATION_COMPLETE_PENDING_MANUAL_VERIFICATION` — every automatable, native-local and review step ran; four human checks remain (M06-V001–V004).

### Local-era discovery: the desktop must be isolated for the suites

This terminal holds the Accessibility grant, which earlier local sessions did not. App-level harnesses that keep the default collector (stuck overlay, lifecycle, normalization, vocabulary, cleanup, insights, hub, scratchpad, insertion pipelines) reached the live trust check 2–24 times each and would have read whatever field was focused. Every suite, probe and benchmark in this session therefore ran under `tests/v2/context/run_isolated.py` (real AppKit/PyObjC; live-desktop Accessibility reads and synthetic events blocked and counted — every recorded run reports `ax_calls_blocked=0`). Native qualification ran only against a synthetic helper window of the session's own, reached by its pid (`tests/v2/context/native_ax_target.py`).

### Reproduction

Every finding was reproduced first on a frozen worktree of `6a083bd` by `scripts/v2/m06_remediation_repro.py` (synthetic AX trees, temporary stores; app probes drive the real `AppDelegate` hotkey path): **25/25 reproduced** (16 defects, 5 test gaps and 4 design concerns measured as conditions) — `acceptance/M06/remediation/repro_base_6a083bd.json`. The native probe (`scripts/v2/m06_native_probe.py`, `native_probe_base_6a083bd.json`) showed the base could never collect nearby text, identifiers or a window title natively: `_as_range` read a real AXValue range as `(None, None)`, a raw CFRange passed to `AXStringForRange` failed with −25201, and `AXFocusedWindow` on a field answered −25205. At the first pass: 24/25 not reproduced (a21 awaited the contract's caller inventory) — `repro_first_pass_545e21b.json`; at the final code: **0/25** — `repro_final_a41dfcc.json`. No finding was refuted. (The `545e21b` record's `code_root_modified: true` was caused by the untracked M11 test file; the stamp counts tracked changes only from `a41dfcc`.)

### Dispositions

| Finding | Disposition | Observed on `6a083bd` | Repair | Regressions (fail on `6a083bd`) |
|---|---|---|---|---|
| 01 allow/deny decision not bound to the objects read (Critical) | reproduced | a focus switch before the field read made the collector read app B's field under app A's allowed decision; a switch mid-collection mixed A's field with B's page | every read goes to one element taken from `AXUIElementCreateApplication(pid)` and checked with `AXUIElementGetPid`; classification before content; destination re-checked after the field read | `test_a01_…` (4) |
| 02 equal-title cache reuse (High) | reproduced | a second capture made no AXURL read and reported the stale origin as `ax_url`; a workspace carried across documents | cache keyed by pid + window element + field element, 30 s, successful `ax_url` reads only, reused row marked `cached`; workspace never cached | `test_a02_…` (2) |
| 03 `same_destination` matches absent/contradictory identity (High) | reproduced | `None == None` bundles matched; equal pids with two different bundles matched | one shared rule: a usable pid on both sides, else a usable bundle on both; contradiction refused; absence never matches — used by M08 `validate_target` and `TargetLease` | `test_a03_…` (2) |
| 04 native boundary masked by test doubles (High) | reproduced natively | see Reproduction | typed native adapter: `AXValueGetValue` decode, `AXValueCreate` boxing, UTF-16 host units, the field's own `AXWindow` with a messaging timeout | `test_native_ax.py` (7), corpus N001–N003/N005/N006 |
| 05 mutable data under a fixed context id (High) | reproduced | nested values rebindable under one id | transitive freeze; detached JSON | `test_a05_…` |
| 06 cancel/delete/quit do not revoke (High) | reproduced | after `abandon` a provider still made a content read; `begin` after shutdown was accepted and 12 blocked collections were admitted; cancel, deletion and quit never touched the collector | job-owned handles; flag-only `revoke`; guarded host refuses calls after revoke/seal; admission cap 4, age cap 30 s; `shutdown()` on quit; app wiring on cancel, short discard, abandon, deletion | `test_a06_…` (3) |
| 07 release budget and clock (High) | reproduced | an injected 200 ms on the release path fell outside the clock (release event → `released_mono` 207 ms); the widened scope build ran synchronously on the release path | clock at `_finishCapture` entry; the widened projection is built while recording; bounded wait; a synchronous build only when the per-entry estimate fits the remaining budget, else an explicit `widening_deferred` downgrade that keeps the captured tuple | `test_a07_…` (4) |
| 08 benchmark green without its work (High) | reproduced | both arms enabled; timer after `press_release`; warm cohort was tuple equality | schema-2 benchmark: work validity first (exit 2), OFF = zero Accessibility calls, injected-delay conservation, per-run disposition gate | four benchmark mutants rejected (mutation check) |
| 09 selection/nearby bounds (High) | reproduced | a 2,000,000-character selection kept whole (snapshot JSON 2.0 M characters); an oversized flank kept 588,889 characters; a negative range recorded | flanks 600 UTF-16 units, over-returns clipped, cut surrogates dropped; selection read only with a known range ≤ 4096 units, else a caret with an omission and no replacement authority | `test_a09_…` |
| 10 config coercion (Medium) | reproduced | the string `"false"` enabled context; a string deny list did not deny its app; an infinite deadline was accepted | `config.context_policy` fails closed; `config.context_invalid` event | `test_a10_…` |
| 11 finalize globally selected (Medium) | reproduced | concurrent finalizes produced two ids | `finalize(coll=…)` on the job's handle, idempotent under the compose lock | `test_a11_…` |
| 12 downstream clearing / wrong bucket (Medium) | reproduced | a second late batch got another id; the late document did not feed the workspace | sealed one-shot `take_downstream`, `late_delta` with `parent_context_snapshot_id`; dependency data from the collection view | `test_a12_…` (2) |
| 13 status honesty (Medium) | reproduced | a cached absence reported `ok`; a window failure relabelled a retained field `provider_failed`; a failed subrole read was classified `text` and its content read | AX error classes (value / absence / permission / failed); positive-only cache; per-provider status; a failed classification is never text | `test_a13_…` (3) |
| 14 origin userinfo / title false positives (Medium) | reproduced | a synthetic userinfo credential survived in the origin; the title `foo.com.TXT` became a host | origin rebuilt from validated parts; title heuristic narrowed | `test_a14_…` |
| 15 optimistic retention (Medium) | reproduced | `retained: true` before the artifact and lease committed | `publish_example` reconciles: no committed artifact or live lease ⇒ `retained: false`, `retention_write_failed`, `missing_reasons.context_snapshot_payload` | `test_a15_…` |
| 16 engine context coupled to vocabulary (Medium) | reproduced | with vocabulary off the engine got no destination; a failed widened build escaped as `context.finalize_failed` after the job already held a finalized snapshot | transactional `_finalize_job_context` with `scope_disposition`; engine context independent of vocabulary | `test_a16_…` (2) |
| 17 privacy oracle incomplete (test gap) | reproduced | the historical suite watched 4 content attributes | an independent oracle over all 9 content-bearing attributes (`m06_world`), reads counted even when discarded | corpus F-DENY-1…5, F-CLASSIFICATION |
| 18 lifecycle races lack latches (test gap) | reproduced | no Barrier/Event in the M06 suites | 22 stateful corpus scenarios and the regressions use Events/Barriers (13 latch uses) | corpus F-STATE-* |
| 19 hostile-context oracle (test gap) | reproduced | no full-request oracle | the corpus inspects the full cleanup request and rendered prompt (`build_messages`, vocabulary pairs) | corpus F-M07 |
| 20 profile/retry matrix (test gap) | reproduced | no end-to-end profile A/B or unscoped-retry identity assertions | ProfileA/ProfileB and `retry_unscoped_default` oracles | corpus F-PROFILE, F-RETRY |
| 21 native + caller coverage unqualified (test gap) | reproduced | no native suite, no caller inventory | `test_native_ax.py` and the “Caller inventory” section of `contracts/context.md` (local `rg`) | — |
| 22 secure/unclassifiable metadata (design) | adjudicated (D1) | a secure element was asked for AXURL, and its origin was retained as `ax_url` | role/subrole (+ ownership, window) only | `test_a22_…` |
| 23 heuristic origin as scope authority (design) | adjudicated (D3) | a title-derived origin selected site entries | title origin is evidence, never site scope (`scope_site_origin`) | `test_a23_…` |
| 24 downstream revision policy (design) | adjudicated (D4) | undefined delta/parent semantics | late delta with parent link, sealed | `test_a12_…` |
| 25 destination distinctions for insertion (design) | adjudicated (D5) | another window under an equal title kept authority | M08 compares the in-memory window element and the title; caret moves and same-window field moves keep authority | `test_a25_…` |

Regression record: `regressions_base_6a083bd.json` **1/31** (the one pass is the A01 positive control) · `regressions_first_pass_545e21b.json` 31/31 · `regressions_final_a41dfcc.json` **31/31**.

**Adjudicated policies** (`m06-policy-v1`, `tests/v2/context/m06_corpus_adjudications.json`, stated in `contracts/context.md` → “Remediation policy”): D1 secure/unclassifiable metadata · D2 failed versus absent (−25205/−25212 absence, −25211 permission) · D3 heuristic authority · D4 downstream revisions · D5 destination distinctions · D6 configuration (read once, fail closed) · D7 budgets · D8 native offsets (code points only when exact; host UTF-16 units always) · D9 lifecycle · D10 retention acknowledgment · D11 release clock · D12 origin forms · D13 workspace locators. The corpus runner scores policy-dependent cases only against this recorded version.

### Independent review round

**Independence method.** The frozen first pass (`545e21b`; production `88ee303`) was reviewed by a separate `code-auditor` subagent started with an empty context, given only the audit, the corpus, the contracts, the diff `6a083bd..545e21b` and a written brief, in its own read-only worktree at `545e21b`. It changed no tracked file (it disclosed refreshed git-ignored `__pycache__` files). Every allegation was re-run by the author with independent probes (`scripts/v2/m06_review_repro.py`) on the first-pass code before any fix: `review_repro_first_pass_545e21b.json` — r1, r2, r3, r4, r6, r7, r8/r9, r10 reproduced. The reviewer's probe sources are not committed (they embed the session's scratch path); the author's reproductions are.

| ID | Allegation | On the first pass | Disposition |
|---|---|---|---|
| R1 (High) | the M08 window element replaced the title check: another tab of the same window kept authority | lease granted, `window: pass` | fixed: element AND title both apply |
| R2 | an unkept (over-budget) selection still claimed replacement authority, so a valid insert was refused | `selection: fail` | fixed: no host range without the kept text; a caret |
| R3 | a deferred/failed widening still upgraded the M10 profile and number policy | `ai_prompt` profile over app-only vocabulary | fixed: the captured tuple is restored on defer/fail; `no_policy` disposition |
| R4 | `_m11_capture_selection` reads the raw `context_denied_apps` | a padded deny entry let M11 read a denied app's selection | **deferred to the M11 merge** — M11-owned code, rewritten on the M11 remediation branch; still reproduced at the final code (declared) |
| R5 | fakes put AXURL on text fields; real browsers may expose it only on the web area | static | **not verifiable locally**: the session's own WKWebView helper exposes no web tree while not active; carried by M06-V001 and the contract's limitations |
| R6 | a title-derived origin was cached under a key without the title | stale site, `cached: True` | fixed: only `ax_url` origins are cached |
| R7 | a late precompute evicted the next job's projection; a global cost figure | eviction observed | fixed: a finalized handle previews nothing; per-entry cost estimate |
| R8 | the window fallback could attach another window | static | fixed: no application-window substitute |
| R9 | no messaging timeout on the window element | static | fixed |
| R10 | −25211 read as `classification_failed`; a recycled pid ignored the bundle | reproduced | fixed: permission; bundle drift detected |
| R11 | mutation checker could not report a clean run; `error` counted as a kill; MUT-10 masked | reproduced | fixed (`9435846`, `5f277d7`); MUT-01 re-targeted to the collector gate (the provider-level gate is dead code) and documented; MUT-04/MUT-12 are killed only by the regression suite (disclosed) |
| R12 | benchmark validity checked on run 0 only; dispositions ungated; `release_immediate` never downgraded | reproduced | fixed: per-run gates and labels |

Fail-first / pass-final: `review_round_regressions_first_pass_545e21b.json` **0/8** (exit 1) · `review_round_regressions_final_a41dfcc.json` **8/8**; final review repro `review_repro_final_a41dfcc.json`: only r4 reproduces (deferred).

### Corpus, stateful, metamorphic and mutation checks

`tests/v2/context/m06_corpus_runner.py` judges every case with oracles independent of the code under test and writes results keyed by case, family, SHA and policy version; the corpus file is never edited. Non-native cases run under desktop isolation; N001–N006 run natively against the helper window.

| Code | pass | fail | not run |
|---|---|---|---|
| base `6a083bd` | 63 | 36 (24 families) | 2 (M005 needs the mutation report; N004) |
| first pass `9435846` | 100 | 0 | 1 (N004) |
| final `3a2d8e1` | **100** | **0** | **1** (N004 — System Settings permission change; M06-V004) |

Stateful scenarios (22, Events/Barriers, including the 16 required) all pass; **metamorphic relations 10/10**; M005 is judged from the mutation report. **Mutation check** (`scripts/v2/m06_mutation_check.py`, disposable copies, each mutation proven applied; a harness error or failing control is never a kill): first pass `mutation_first_pass_9435846.json` and final `mutation_final_3a2d8e1.json` — control green, **18/18 killed**, **4/4 benchmark mutants rejected** (exit 2: OFF arm forced on, clock after release work, no-op scope upgrade, omitted hint packaging). At `a41dfcc` MUT-14's anchor no longer matched after R6 changed the guard (reported as a harness error, not a kill); `3a2d8e1` re-anchors the unchanged mutation, killed by LF-M06-E003 and the regression suite. MUT-04 and MUT-12 are killed only by the regression suite.

### Verification — portable, shim and native (kept distinct)

- **Whole tree, desktop-isolated, real PyObjC** (`sweep_*.json`; model-backed files excluded and run separately): base 80/80 exit 0 · first pass 82 files, 81 exit 0 · final `a41dfcc` 83 files, **81 exit 0**, 861 `ok` case lines, 0 `FAIL` lines. The two non-zero files: `test_native_ax.py` exits 2 (NOT RUN) under isolation by design and ran natively as its own record; `tests/v2/ui/test_training_review_hub.py` aborted once (SIGABRT 0.6 s in: an AppKit assertion in `-[NSView _setHidden:]` on the `localflow-hub-query` thread — the suite replaces `AppHelper.callAfter` to run inline, so a refresh runs off the main thread) and passed 6/6 alone at the same SHA and in every other sweep; M06 changed nothing under `localflow/v2/ui` or `tests/v2/ui` — a pre-existing M14 test-harness race, recorded, not repaired here.
- **Targeted seams (final sweep, ok lines):** M06 snapshot 27, pipeline 12, remediation 31, review round 8 · M05 remediation 28, app seams 12, review round 31, vocabulary store 16, pipeline 6, hint selection 9 · M02 store 17, remediation store 19, collector 11, remediation collector 14 · M03 remediation app 14, supervisor 17, worker 10, journal 6, review round 15 · M07 cleanup pipeline 13 · M08 insertion attribution 10, pipeline 5, races 13 · M10 profiles pipeline 13, style resolution 9, snippets 9 · M14 learning 11.
- **Declared shims:** the synthetic AX host (`tests/v2/context/m06_world.py` `WorldHost`, `test_context_snapshot.FakeAXHost`) stands in for Accessibility in the regressions, the review round, the corpus's non-native cases and the context suites. These certify policy and ownership logic, not native behaviour.
- **Native (real Accessibility API, own helper window):** `test_native_ax.py` **7/7** at first pass and final (AXValue decode and boxing, UTF-16 offsets with exact code-point conversion, `CFEqual` on element refs, a dead process's element, the M08 range bridge); native probe `native_probe_final_a41dfcc.json`; corpus N001–N003, N005, N006 pass, N004 not run.
- **Model-backed:** `tests/v2/training/test_live_pipeline.py` **2/2** at `3a2d8e1` (real Qwen cleanup capture; real Parakeet over synthetic no-speech audio), models above, offline — `live_pipeline_final_3a2d8e1.json`. M06 privacy and ownership need no model; this run covers the training/store capture seam M06 changed.

### Reference-Mac performance (`docs/v2/benchmarks/20260926-043413-m06-remediation/`)

Code `3a2d8e1` (production `4388b52`), clean code paths, AC power, no thermal or performance warning recorded, load average 3.1–3.5 with another session's LectureLive test binary on one core (not stopped; disclosed). 10,000 entries: 9,500 global + 500 workspace-scoped plus authored contenders. **`benchmark_m06.py`: work valid, verdict pass (exit 0).** Timers (ms):

| Cohort | n | p50 | p95 | p99 | Boundary |
|---|---|---|---|---|---|
| **additional delay (gated ≤ 75)** | 60 | 5.5 | **40.5** | 43.2 | release-path ON run − OFF median |
| release path ON | 60 | 16.6 | 51.6 | 54.3 | hotkey release event → job enqueued for ASR; 300 ms recording; projection cache cleared before every run |
| release path OFF | 60 | 11.1 | 11.9 | 53.7 | same boundary, context off (zero context Accessibility calls) |
| release path, projection cached | 20 | 13.7 | 30.0 | 30.0 | same boundary |
| release → ASR handoff | 20 | 21.5 | 35.1 | 35.1 | release event → worker `transcribe()` |
| identity capture | 180 | 0.004 | 0.006 | 0.02 | `capture_identity()` |
| finalize, providers done | 60 | 0.023 | 0.048 | 0.064 | `finalize(handle)` |
| finalize, slow provider cut | 20 | 85.1 | 85.1 | 85.1 | 75 ms deadline + timer wake slack (`Event.wait(0.075)` idles at ~82 ms on this Mac) |
| cache reuse / invalidation / drift refusal / bounded payload | 60/60/60/10 | 0.02 | 0.03 | ≤ 0.06 | `finalize(handle)` |
| widened projection build | 10 | 59.2 | 77.8 | 77.8 | built while recording, off the release path |
| hint selection + serialization | 10 | 7.4 | 20.6 | 20.6 | fresh widened snapshot |
| M06 finalize / M10 upgrade components | 60 | 3.9 / 0.07 | 4.2 / 0.08 | 4.7 / 0.09 | inside the release path |
| injected 50 ms | 10+10 | — | — | — | appeared as 50.9 ms: the clock covers the release path |
| release immediately after press (ungated) | 20 | 190.0 | 203.0 | 203.0 | dispositions: widened 6, `widening_deferred` 14 |

The last row is a stress cohort the app cannot reach from a real dictation: a release shorter than `min_duration_sec` (0.3 s) is discarded before any context finalize (`app.py` `_finishCapture`), and the harness feeds audio regardless. It shows the explicit downgrade being taken, and that an immediate release still costs ~190 ms while the projection build is unfinished; not investigated further here.

**M05 scope upgrade and packaging, and the 75 ms disposition.** `benchmark_m05.py --entries 10000` at the same SHA (`m05/m05_benchmark.json`; its fixed label still reads “algorithmic evidence only” from its cloud origin — this run is on the reference Mac; its `code_root_modified: true` counts every tracked file, and at run time only `docs/v2/VERIFICATION.html` and `docs/v2/contracts/context.md` had uncommitted edits): work valid, verdict pass; cached retrieval p95 0.016, fresh selection 3.55 / scoped 3.46, normalize 4.72 no hit / 4.66 with hits, dense hits 8.2, shared-first-word 2.1, long aliases 2.8, cold selection 17.0, cold snapshot 160; **`scope_upgrade_and_packaging` p50 121 / p95 141 ms** against its 75 ms bound. As a stand-alone synchronous build it still exceeds 75 ms; M06 no longer runs it on the release path — the projection is built during recording, and the release path's measured addition is p95 40.5 ms. The 75 ms post-release context target is met on the release path and was not redefined. The cloud figure (364 ms) and the September 22 M06 figures (cold scope 62.2 ms, fast finalize 0.303 ms) describe other code and stay as recorded. M05-V003 (code `01a1f3c`) remains the owner's to record.

### Contracts changed (and why)

`contracts/context.md` states the repaired design: read ownership, lifecycle/revocation/bounds, release budget, sensitive-field rules, budgets and offsets, freshness/identity/caching, degradation statuses, configuration, evidence, hosts, the caller inventory, limitations (including the field-only `AXDocument` read) and the `m06-policy-v1` decisions. Interface addenda: `targets.md` and `insertion.md` (the shared identity rule, window element + title, host-unit selection), `vocabulary.md` and `profiles.md` (`scope_site_origin`, `scope_disposition`, captured tuple on downgrade), `training_evidence.md` (retained means acknowledged), `INDEX.md`. No frozen identifier changed; the envelope additions are additive; no ADR.

### Historical records (2026-09-22)

The September 22 M06 results, benchmark (`benchmarks/20260922-115621-m06/`) and pending item `native-m06-destination-trial` are preserved unchanged in `results.json` and above; this addendum supersedes only their claims about the current code. The pending item is carried by M06-V001 (destinations, selection, continuation) and M06-V002 (password field, denied app) under instruction revision M06-r1.

### Pending manual verification (`docs/v2/VERIFICATION.html` → M06, instructions M06-r1, code `4388b52`)

Front-end edit made after invoking `frontend-design:frontend-design`; only the M06 placeholder section and the “Last updated” line changed — M01–M05 IDs, revisions, statuses, the storage key and the page script are untouched. **M06-V001** real destinations: TextEdit, Safari, Chrome, a selection — records whether each browser exposes the page address on the field (R5) and whether TextEdit exposes its document on the field · **M06-V002** a real password field and a denied app (Notes) · **M06-V003** switching window or tab between release and insertion, a moved caret, a control · **M06-V004** Terminal's Accessibility switched off and on (corpus N004). All start **Not run**. A closing note links the M05 benchmark above to M05-V003 without recording it.

### Limits

Declared residuals: R4 (M11's raw deny-list reader; case-sensitive deny matching) until the M11 merge · browser page addresses may be exposed only on the web area, not the field (R5) — then site-scoped entries do not apply in that browser until a bounded ancestor walk is qualified natively · `AXDocument` is read from the focused element only · an immediate release costs ~190 ms while the projection build is unfinished (unreachable below `min_duration_sec`) · the slow-provider cut lands at ~85 ms (75 ms deadline + platform timer slack) · MUT-04/MUT-12 killed only by the regression suite · M08 does not distinguish two same-role fields within one window (by design) · the M14 hub suite's off-main-thread refresh race. The installed /Applications app is not rebuilt (no build authorized).

### Privacy

Synthetic fixtures, synthetic helper windows and temporary stores only; no installed app, real destination, private text, browser history, dictionary, audio or screenshot was read or committed. Committed evidence carries ids, counts, statuses, timings and synthetic strings; a scan of the branch diff and every evidence file found no user paths. The reviewer's probe sources stayed in scratch because they embed the session's scratch path.

### Orchestration overview — not realigned

`ORCHESTRATION.html` was left untouched: the unmerged M11 remediation branch (`origin/claude/localflow-m11-remediation-spdq35`, commit `1be42ee`) rewrites the same NOW verdict, should-run paragraph and open-threads rows, so an edit here would conflict. What a realignment after both merges should change: the M06 strip — remediation state `LOCAL_REMEDIATION_COMPLETE_PENDING_MANUAL_VERIFICATION`, final production `4388b52`, benchmark “additional post-release delay p95 40.5 ms / slow-provider cut ~85 ms” in place of the September 22 figures; the human-verification ledger — `native-m06-destination-trial` carried by M06-V001/V002, with M06-V003/V004 added; open threads — R4 (M11's raw deny-list reader, owned by the M11 merge), R5 (browser page address on the field, owned by M06-V001), the M14 hub suite's off-main-thread refresh race (M14); should-run order — merge the M11 remediation before, or re-check R4 against, this branch.

### Merge readiness

Code: ready to merge from this session's point of view — all 25 findings are repaired or adjudicated with regressions that fail on the audited base, every confirmed review finding is fixed with a regression that fails on the first pass (R4 deferred to the M11 merge, R5 carried by M06-V001), the corpus is 100/101 with the one human case pending, 18/18 mutations are killed, the benchmark passes with valid work, and no targeted downstream regression was found. Native certification is not given: M06-V001 (especially what real browsers expose) is the most informative first check. Recommended merge order: after any M11 remediation merge that rewrites `_m11_capture_selection`, or with R4 re-checked against it.

## Post-integration note — 2026-09-26 (pre-M08 canonical integration, reference Mac)

**What changed around M06.** The M06 branch head `cb05ca9` (production `4388b52`) is the base of `integration/m06-m11-pre-m08`, fast-forwarded to `main`; the M11 remediation was replayed onto it (`1545de9`), not merged from its stale base. Nothing recorded above is rewritten: every figure in the remediation addendum still describes `3a2d8e1`/`a41dfcc`.

**R4 closed.** `_m11_capture_selection` now takes the M06 decision: `snapshot.app_denied` (trimmed, case-insensitive — Apple defines `CFBundleIdentifier` as case-insensitive) over the validated `_context_policy` list, before any Accessibility call; a malformed deny list refuses every app for transforms (`deny_list_invalid`). The collector's identity decision uses the same function, so a differently cased deny entry now also denies its app for context (strictly more denial; no corpus or regression expectation changed). Repair `46ecd5b`; the capture also records host-unit ranges and the field's own window element, so a strict transform accept applies the R1 element-and-title rule. Regression `tests/v2/transforms/test_selection_capture_m06_seam.py`: 2/8 on the historical M11 head `1be42ee`, on this branch's head `cb05ca9` and on the replay `1545de9`; 8/8 integrated (`acceptance/M11/integration/seam_regression_four_roots.json`). The review probe `r4` now calls the real capture (`cbf5ae4`): reproduced at `1be42ee` (7 Accessibility calls) and `cb05ca9` (4), not reproduced integrated (0 calls, `app_denied`).

**M06 evidence on the integrated tree** (code `f8bf7e5`; production `9a049b3`), in `acceptance/M06/remediation/*_integration_f8bf7e5.json`: regressions 31/31, review round 8/8, context snapshot 27, pipeline 12; review repro — none of r1–r10 reproduced; corpus **100 pass / 0 fail / 1 not run** (N004, M06-V004), metamorphic 10/10; mutation check control green, **18/18 killed**, **4/4 benchmark mutants rejected**; native `test_native_ax.py` **7/7**; model-backed `test_live_pipeline.py` **2/2** (same model snapshots as above, offline); whole-tree desktop-isolated sweep 86 files, 85 exit 0 (the native suite exits 2 by design), 928 `ok` lines, 0 `FAIL`, 0 Accessibility calls blocked anywhere. One mutation run's control failed on native case N001 while keyboard input was being typed at the Mac (the failing reads returned caret positions, not the probe's selection); interleaved reruns then passed 10/10 on both `f8bf7e5` and `cb05ca9`, and the recorded run is the clean rerun.

**Unchanged.** R5 (a browser page address exposed only on the web area) remains open under M06-V001. M06-V001–V004 remain Not run at instructions M06-r1 — the runbook's M06 section is untouched. The M14 hub off-main-thread race did not reproduce in either integration sweep and stays an M14 thread. The reference-Mac benchmark figures above (additional post-release delay p95 40.5 ms; slow-provider cut ~85 ms = 75 ms deadline + platform timer slack) were not re-measured: the integration leaves the release path unchanged — its one change on the dictation path is the identity decision at press (`app_denied`, a comparison over the deny list).
