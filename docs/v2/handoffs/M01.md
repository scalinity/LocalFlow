# M01 Handoff — Reconcile the deployed baseline and freeze evaluation

**Milestone:** M01 · **Requirements:** LF-R01, LF-R28 · **State:** `implemented_pending_human`
**Base commit:** `7cdd90486b23a1a86b3da00f9fdee39bfa40a8f9` (branch `main`, remote `scalinity/LocalFlow`)
**Tree at start:** clean except untracked `docs/` (the v1.1 planning package). No other worktrees; no other session owned any file M01 touched.
**Tree at end:** M01 adds `docs/v2/{START_HERE.md,STATUS.json,registry.json,baseline/,contracts/,acceptance/M01/,handoffs/M01.md,benchmarks/}`, `tests/v2/`, `scripts/v2/`, `.gitignore` rules. No tracked file other than `.gitignore` was modified; no `localflow/` production code changed.

## Deployed/source/config/data findings

1. **Installed app == Git HEAD.** `/Applications/LocalFlow.app` (built 2026-08-11 11:35, id `com.danny.localflow`, v0.1.0/1) embeds code byte-identical to `7cdd904`. Nothing local is newer than the snapshot *in the bundle*.
2. **A locally-built feature set is newer than — indeed absent from — Git, and is no longer deployed.** `stats.db`, `dictionary.json`, `transforms.json` in Application Support were produced on **2026-07-04 17:08–17:41** by modules (`localflow/stats.py`, `transforms.py`, `profile.py`, `_compat.py`, `Desktop/dashboard.py`) whose sources were deleted before the snapshot. Only compiled `.pyc` remnants survive (in ignored `__pycache__` dirs; interfaces recovered and listed in `baseline/manifest.json`: `dictations` schema matches the live db; TransformManager did selected-text transforms via clipboard; a WKWebView Dashboard with a stats JS bridge). The current bundle contains none of it, so the running app no longer writes these files — **treat the data as live user state to import and extend (M02/M05/M11), never as absent features.**
3. **All three uploaded artifacts are this machine's live data, unmodified:** live `stats.db`/`dictionary.json`/`transforms.json` hash-match the audited values exactly; the audited `LocalFlow.log` (544,221 B, `51e8ee70…`) is a byte-prefix of the live log (now 546,689 B; the app is running and appending).
4. **Effective config:** no `$LOCALFLOW_CONFIG`, no Application Support override → the repo/bundled `config.json` wins. ASR `mlx-community/parakeet-tdt-0.6b-v3`; cleanup `llm` via `mlx-community/Qwen3-4B-Instruct-2507-4bit` — the confirmed 4B, verified running: the live log's current instance shows `cleanup model loaded (Qwen3-4B-Instruct-2507-4bit)`.
5. **Model caches:** effective cache is the default `~/.cache/huggingface/hub` (no env overrides in the running process or launchctl). User-stated provenance: weights originate from `~/Documents/LocalAI/models/hub`, which holds **byte-identical copies at the same revisions** (parakeet `ed2b7e8c…`, qwen `50d42775…`; weight SHA-256s recorded in the manifest).
6. **Runtime:** Python 3.14.7 (Homebrew), mlx 0.31.2, mlx_lm 0.31.3, transformers 5.10.4 (the `<5.11` pin holds), numpy 2.4.6, sounddevice 0.5.5. Hardware: Apple M5 Pro, 48 GB, macOS 27.0 (26A428); probe ran on battery.
7. **Data locations:** analytics `stats.db` + `instance.lock` (no producer in any surviving source; inferred — not proven — to belong to the deleted Jul-4-era modules and recorded as unresolved in the manifest), dictionary/transforms JSON, log at `~/Library/Logs/LocalFlow.log`, last-5 audio dumps `~/Library/Logs/LocalFlow-audio/`. Private evidence root: `~/Library/Application Support/LocalFlow/v2-evidence/` (outside Git; the snapshot tool refuses roots inside the repository).
8. **Unresolved (with reason):** exact producer source for stats/dictionary/transforms — deleted; only bytecode remnants remain. Interface recovery suffices for the M02 import contract (which reads the data files); full decompilation is not needed. Recorded in `baseline/manifest.json` `unresolved`.

## Historical evidence reproduced

Against the hash-verified snapshot (`51e8ee70…`) with `scripts/v2/parse_legacy_log.py` (E02 protocol): **749** pairs · **477** timed · cohorts **476/272/1** · **498** audio diagnostics, 0 overflow-positive, 10 zero-voiced · **3/3** Metal shared-event failures · 21 load-failures (all `'str' object has no attribute '__module__'`) · 27 load-successes · 29 empty · 9 sanity fallbacks · 11 warnings · 16 unpaired timings · 1 unconsumed audio · sums 176.6 s ASR / 629.6 s cleanup · r=0.94888 word-count↔cleanup-time · band sizes 132/189/116/32/8 · 11/11 cited regression windows located (DOM deletion at lines 1540–1544, reply-request deletion 1790–1794, instruction-executed 1690–1694, Metal failures 3466–3474, …). Two descriptive heuristics differ by one from the historical JSON (lexical 107 vs 108; boundaries 134 vs 133) — definitions not pinned by E02; recorded, not forced.

## Contracts and decisions established

- `contracts/`: `jobs`, `events`, `artifacts`, `targets`, `metrics`, `asr_hints`, `training_evidence`, `references`, `preferences`, `dataset_exports` (+ `INDEX.md` with binding privacy rules). M01 froze the **identifier/provenance core**: stable IDs (`job_id`/`family_id`/`example_id`/`revision_id`/`consent_revision_id`), legacy import identity `legacy:<source-sha>:<line-range>` with `time_quality:"unknown"`, null-with-reason vocabulary, four reference kinds, partition-vs-tag splits, test-manifest reservation. Producing milestones add fields; they must not repurpose identifiers.
- `registry.json`: 33 requirements / 22 suites, machine-derived from the current S05/E08 tables (regenerate + cross-check via `scripts/v2/build_registry.py` / `tests/v2/test_registry.py`).
- Fixture families reserved per E04 (`tests/v2/fixtures/manifest.json`): 726 planned cases across 9 families, zero claimed; test manifest reserved with zero families assigned (S29.11).
- Source-document hashes at M01 time: the v1.1 package's own `README.md` §7 records the v1.0 canonical hashes; of this repo's six package files, the three Markdown documents are the canonical ones read (no `SHA256SUMS.txt`/`.docx` editions are present in the repo — recorded here as a package-completeness fact, not a blocker).

## Verification (commands, results, kinds)

Full detail in `acceptance/M01/results.json`. Summary — all exit 0 unless noted, cwd = repo root:

- `tests/v2/test_legacy_parser.py` — automated — 5/5 incl. audited-aggregate reproduction.
- `tests/v2/test_registry.py` — automated — 33/22 traceability holds.
- `tests/v2/test_baseline_manifest.py` — automated — provenance fields + privacy scan + reserved-only fixtures.
- `tests/v2/test_snapshot_safety.py` — automated — snapshots never mutate originals.
- `scripts/v2/snapshot_user_data.py` — automated — consistent snapshots; all sources unchanged.
- `tests/test_stuck_overlay.py` — automated — 5/5.
- `tests/test_cleanup.py` — **model-backed native Mac** — **51/51** (Qwen3-4B; avg 0.94 s/case; 2 recorded FALLBACK passes).
- `tests/test_cleanup.py --basic` — automated — 39/51, **pre-existing**: the basic tier implements neither corrections nor formatting; not a regression (no production code touched).
- `scripts/v2/baseline_probe.py` — **model-backed native Mac** — stage timings on synthetic input: ASR load 3.79 s / transcribe 0.44 s cold, 0.041 s warm; Qwen load 1.96 s / clean(28 words) 0.98 s (`benchmarks/20260921-180858-m01-baseline/probe.json`). Stage-only, not release-to-insert.

## Human verification

The spec's minimum manual check — launch the installed app and confirm the deployed cleanup model/readiness — **was established without manual interaction**: the app is running (PIDs recorded) and its own log proves Parakeet ready + `Qwen3-4B-Instruct-2507-4bit` cleanup loaded in the current instance. No UI playtesting was requested or performed. **Pending human:** the release-to-insert latency pilot (needs a real speaker + instrumented target; earliest gate owner is M15; does not block M02).

## Privacy

Nothing private entered Git: committed artifacts carry hashes, counts, line locators and paths only; `test_baseline_manifest.py` fails on private-bearing file types under `docs/` or `tests/`; `.gitignore` extended (`*.db`, `*.sqlite`, `LocalFlow.log`, `docs/v2/private/`); snapshots live outside the repo under `v2-evidence/`. No credentials exist anywhere in the inspected tree.

## Limitations / deviations

- No deviations from the M01 spec. No ADRs (nothing accepted against the spec).
- The two ±1 descriptive-heuristic deltas above; `unconsumed audio` and cohort semantics implemented per E02 and validated against the artifact.
- ORCHESTRATION.html does not exist in this repository; per instructions, none was created.

## /review-2 → /address record

`/review-2` ran two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Both verdicts: **Approve with Suggestions** — zero critical, 7 warnings + 14 suggestions after dedup. Highlights: the parser could drop a complete pair when an unrelated record interleaved between the raw and cleaned prints, or when a snapshot ended mid-pair (reviewer-confirmed by execution; the hash-pinned audited artifact was unaffected); the manifest generator could claim a bundle match on a diff error, crash instead of recording null-with-reason, and hard-code model IDs plus a static note that could contradict its own computed result; the privacy scan was extension-only against the snapshot tool's own artifact names; a two-word verbatim transcript quote sat in the committed reconciliation JSON; the snapshot CLI flow was untested; the probe could crash before writing its report.

`/address` fixed every valid M01-scope finding in-session (parser orphaned-raw rescue + EOF flush with aggregates re-verified identical; diff exit-code discrimination + stderr; null-with-reason config reads; model IDs/note derived from the computed effective config; `.gitignore` extended incl. `LocalFlow.log.*`/`v2-evidence/`; content-marker privacy scan; snapshot CLI argv/in-repo refusal/nonzero exit + two new tests; quote reworded and JSON regenerated; probe resilience + honest mode labels; registry drift warnings + 2+-digit IDs; chunked hashing; UTC run id; file-handle hygiene). **Deferred to M02** (recorded in `.gitignore`): a `!tests/v2/fixtures/**` negation when synthetic fixture databases arrive. **No findings rejected as incorrect.** Post-address re-run: all v2 tests green (parser 5/5 with audited reproduction, registry 33/22, manifest+privacy incl. content markers, snapshot 6/6), `tests/test_stuck_overlay.py` 5/5, live-data snapshot exit 0 with all sources unchanged; registry and manifest regenerated. Full detail in `acceptance/M01/results.json` `review_loop`.

## M02 eligibility

**Dependency-eligible: yes.** M01 delivered the baseline manifest, contract identifiers, registry, fixture reservations, private-evidence conventions and the verified legacy aggregates M02's import and dated-logging work needs. The pending release-to-insert pilot does not gate M02.

## Notes for the next session

Preserve the Application Support data and the pyc remnants (do not `git clean`); import, never rebuild, the seven rows/terms/definitions. The running app may append to the log mid-session — snapshot before parsing. Rebuild `baseline/manifest.json` with its generator rather than editing by hand.
