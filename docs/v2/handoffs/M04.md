# M04 Handoff — Typed numeric and spoken-syntax normalization

**Milestone:** M04 · **Requirements:** LF-R06, LF-R07, LF-R28, LF-R30 · **State:** `implemented_pending_human`
**Base commit:** `adc872bd0c34b4f38e9c1a5f3f99ce8b31d9c1ab` (branch `main`, remote `scalinity/LocalFlow`)
**Tree at start:** clean (M03 committed at `adc872b`; verified clean before changes). No other worktrees; no other session owned any file M04 touched.
**Tree at end:** M04 adds `localflow/v2/normalize/` (`__init__, engine, numbers, syntax, policy, scoring, span_types` + `policies/number_profiles.json`), `tests/v2/normalization/` (4 suites + `fixtures_numeric.json` 80, `fixtures_syntax.json` 60, `short_command_corpus.json` 24), `scripts/v2/benchmark_m04.py`, `docs/v2/{contracts/normalization.md, acceptance/M04/, benchmarks/20260922-012259-m04/}`; modifies `localflow/{app,config}.py`, `localflow/v2/training.py`, `tests/v2/{test_baseline_manifest.py, fixtures/manifest.json}`, `docs/v2/{STATUS.json, contracts/INDEX.md, contracts/training_evidence.md}`. The worker protocol, capture journal, cleanup adapter and all M01–M03 artifacts are otherwise untouched; the installed app is untouched (no build authorized).

## What shipped

1. **The normalizer (Spec S10, EV-06).** `localflow/v2/normalize`: a deterministic, model-free, stdlib-only package. Typed grammars for integers (technical contexts, prose guards), decimals/signs (exact `Decimal`, significant zeros preserved), percentages and percentage points, currency (locale symbols, `12.000€` under es-ES, word kept when the locale lacks a symbol), month-name dates (invalid days flagged, numeric dates never touched), clock times (no invented meridiem), codes/phones (leading zeros preserved, no country code), versions (anchored, components validated, unanchored multi-point flagged), IPs/ports (invalid octets flagged and region-locked), dimensions/units (`10 × 20 cm`, MB vs Mb distinct in evidence), identifiers (context-fed only), dotfiles, anchored spoken paths, spoken domains/email (known TLDs), registered slash skills (exact spelling, multiword aliases, unknown → literal + retained review suggestion), symbol commands with idiom/noun-compound guards, Markdown structure commands, shell flags/pipes (text only), and the literal escape ("write the word slash" → `slash`).
2. **Typed-span precedence and the edit ledger.** Layers: literal escape → protected syntax (quote zones; already-written forms cannot re-match) → registered skills → typed grammar → context vocabulary. Conflicts resolve by `(layer, longer span, position, class)` — never substitution order; overlapping or differing-output same-span proposals are rejected and retained with reasons (`overlap_conflict`, `ambiguous_same_span`, `unknown_skill`, `invalid_octet`, `invalid_day`, `invalid_version`, `unanchored_version`, `protected_span`, `flagged_region`). All offsets are zero-based half-open Unicode code points. `replay(input)` reproduces the output from the ledger alone; `is_idempotent(policy, context)` reports the honest bool.
3. **Pipeline integration (parent process).** `app.py`'s coordinator thread runs normalization between ASR and cleanup (`transcribing → normalizing → cleaning`); cleanup receives the normalized text; cleanup-off applies the normalized text; any parser exception passes the raw transcript through (`normalization.failed` event, `raw_passthrough`); a policy-load failure disables the stage with an event, never a startup crash. Config: `normalization_profile` (`technical` default | `standard` | `off`), `normalization_locale` (`en-US` default, `es-ES` supported). The M03 worker protocol is unchanged.
4. **Training evidence (S29.4 family, EV-19).** `on_normalization_result` writes the normalized-text and full-ledger artifacts (parent = raw transcript, lease-governed) and populates the envelope's `normalization` block: policy revision (hash of canonical policy-file content + locale + profile + registrations), per-edit ops/values/units/spans, counts, idempotence (evaluated with the pass-1 context; null-with-reason on failure). Envelope values are typed numerics only — string-valued command classes keep their strings in the ledger artifact (S29.14). Without the stage, the honest `not_captured_at_stage` reason remains.
5. **Fixtures and scoring.** Exactly 80 numeric + 60 syntax owning fixtures (34 negative controls; the two literal-escape idempotence corners declared per-fixture), 24 short-command text counterparts, the ITN-independent `score_command_corpus` (exact-token accuracy 16/16, false-command rate 0/8) and `separate_scores` (number-word→digit apart from other edits), and the `preview_phrase` test-phrase API for the future Dictionary/Developer UI.

## Contracts and decisions

- New `contracts/normalization.md` (placement, policy/context objects, ledger schema, precedence, the full grammar behavior reference including guards and documented limitations, evidence fields, M05/M06 dependencies); `contracts/INDEX.md` row + decision note; `contracts/training_evidence.md` M04 live status. No frozen identifier repurposed; registry unchanged (33/22); no ADRs — no spec deviation accepted.
- **Where normalization runs (settled, per the kickoff):** the parent process, coordinator thread. S06's subprocess isolates Metal/MLX faults; normalization holds no MLX state, so it gains no protocol op and never queues behind the supervisor's GPU lock. The S24 budget is met with 5.9× headroom.
- **The literal-escape/idempotence corner:** an escape whose object is or contains a command word/phrase ("write the word comma" → `comma`) is not stable under a second full pass; the pipeline re-normalizes only raw ASR output, both corner fixtures declare `idempotence_expected: false`, and `is_idempotent()` reports it per record. Everything else is idempotent by construction (grammars match spoken word forms only).
- **Unanchored slash chains stay prose** ("slash the budget") — spoken-path conversion is anchor-gated; full path dictation waits for M06 destination context.
- ES scope honestly bounded: numbers/percent/currency/dates covered; ES punctuation command names and 'punto'-joined versions are documented limitations (fixtures LF-SYN-059/060 assert the literal outcomes).

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M04/results.json`. All cwd = repo root, all exit 0:

- **Pre-change baselines** (before any edit): 4× M01 suites, event writer 13/13, store 17/17, import 7/7, collector 11/11, 4× M03 lifecycle suites, stuck overlay 5/5, live pipeline 2/2 (model-backed), cleanup 51/51 (model-backed, 46.3 s).
- **Post-implementation and post-review:** the four new M04 suites — `test_normalize_numbers.py` (80 fixtures + typed values, decimal exactness, conflict retention, standard/off profiles, review regressions), `test_normalize_syntax.py` (60 fixtures + AC02/AC04/AC06, escape corner, preview API, review regressions), `test_short_command_scoring.py` (16/16 exact, 0/8 false commands), `test_normalization_pipeline.py` (real coordinator: stage order, normalized text into cleanup, cleanup-off, off-profile, parser-crash passthrough, evidence envelope + ledger replay through the store, M02-path missing reason) — plus every pre-change suite re-run green and both model-backed suites green post-review (live 2/2; cleanup 51/51, 48.1 s).
- **Benchmark** (`docs/v2/benchmarks/20260922-012259-m04/m04.json`, re-run on post-review code, no model call): short p95 0.222 ms, medium 1.071 ms, long_500 p95 **4.273 ms** (target ≤ 25 ms), long_500_plain (no-match prose) p95 3.418 ms.

## /review-2 → /address record

`/review-2` ran two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 3 criticals, 7 warnings, 16 suggestions after dedup. The criticals: bare "hundred" after quantifiers corrupted prose on the default profile ("a few hundred people" → "a few 100 people"); the "period" symbol command fired on noun compounds ("grace period" → "grace."); and the AC04 static scan passed vacuously — its package path pointed at a nonexistent directory, masked by an `open(` token that would have failed a real scan (evidence corruption: green meant unexamined).

`/address` fixed every critical and every warning in-session with regression tests: quantifier guards (integer grammar + centrally in `_quantity_before` so currency/percent cannot fire either); a `blocker_prev` noun-compound guard list for guarded symbol names; the AC04 scan path fixed, the loader switched to `Path.read_text`, and a non-empty-scan assertion naming the seven expected modules added (the scan now verifiably scans); the app policy fallback made crash-proof (stage-off + event, `None`-guarded call sites); the time grammar gated on time context ("chapter five thirty two" stays prose); banned-token list extended with word-boundary matching; idempotence evaluation carries the pass-1 context with null-with-reason; envelope values restricted to typed numerics so no transcript-derived strings outlive the leases; the manifest invariant made generic over `*_created` blocks; dead code removed. Eleven suggestions applied (from_config raises instead of silent remapping, "through"/"comma" stop words, content-hashed policy_revision, outputs-based same-span rule, profile-driven port anchor, `_optional_year` helper, test-scaffolding cleanup, benchmark plain-prose size, the split M01 sentence in training_evidence.md restored, synthetic fixture email, MappingProxyType immutability). Five deferred with reasons (glued-quote numbers, pointless version flag, inert-until-M05 skill suggestions, consecutive-block spacing, ContextSnapshot M06 fields kept as the documented interface). **No findings rejected as incorrect.** Post-address, every suite and both model-backed checks re-ran green and the benchmark was re-run on the fixed code. (The /address per-fix-commit cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's explicit constraints.)

## Human verification (pending; steps in results.json)

Dictate in the dev pipeline from Terminal.app: a version, a percentage, an IP address, and "slash the budget" across the technical and standard profiles; inspect one collected example's normalization ledger. Native checks remain pending from M03 and M01 as recorded in their results files.

## Limitations / unresolved

- Documented grammar limitations (contract §grammar behavior reference): fractional quantities ("five and a half percent") stay words; terminal bare quantities and year-speak stay words; bare hour+meridiem ("eight AM") and "noon" unsupported; ES punctuation commands and ES 'punto' versions unsupported; unanchored slash chains and bare 7-digit runs stay prose by safety design; the literal-escape idempotence corner above.
- No speculative M06 context implemented: `ContextSnapshot(destination_app, path_context, identifiers)` is the documented minimal interface; only `identifiers` is read today.
- Unknown from M01–M03, unchanged: producer source of stats/dictionary/transforms; producer of `instance.lock`; the M01 release-to-insert pilot (M15 gate). The installed app still runs pre-M02 code (no build authorized); M04 exists in dev runs and tests.

## Privacy

Nothing private entered Git: fixtures are synthetic (origin-tagged, plausibly-real address swapped for the synthetic form); the baseline-manifest privacy scan passes over every new path; envelope values are typed numerics only; the ledger and normalized-text payloads live as lease-governed store artifacts under Application Support; events stay content-free.

## M05 eligibility

**Dependency-eligible: yes.** M04 delivered the typed-span/ledger interfaces and the registered-skill surface M05's dictionary feeds (`NormalizationPolicy.registered_skills`, `preview_phrase` for the Dictionary UI). Pending human checks (M04 dictation profiles; M03 native items) do not gate M05's automatable work.

## Remediation addendum — 2026-09-25 (cloud session, post-audit)

**Input:** read-only audit “LocalFlow V2 — M04 Read-Only Deep Audit” at `1c9d981b1505b6cd975ce6e701e0382c6db96c78` (verdict C; 1 Critical, 12 High, 3 Medium, 1 Low, 4 test gaps, 3 design concerns) and its authored adversarial corpus (368 text cases / 171 families + 10 stateful probes, all `NOT_RUN`). **Inherited base:** `origin/main` = `1c9d981` (main had not moved since the audit; accepted M01 `3db7406`, M02 `bfbc63c`, M03 `3e0d1ab` and the M03 evidence head `1c9d981` verified as ancestors; the M01–M03 sections of `VERIFICATION.html` present). **Branch:** `claude/eloquent-curie-syb79l` (the session's designated remediation branch, created from `1c9d981`). **Production repair:** `d31245f` (first pass, frozen for review) + `4ef219c` (independent-review round) — **final code revision `4ef219c`**; `7ce7460` (reproduction tool) and `08ddf44` (first-pass evidence) are tools/evidence only, and every commit after `4ef219c` is evidence and documentation only. **Environment:** Claude Code cloud container (Linux x86_64), CPython 3.13.12 + numpy 2.5.3, SQLite 3.45.1 — not the reference Mac; nothing native, model-backed, installed-app or Application-Support related was executed or is claimed. **State:** `CLOUD_REMEDIATION_COMPLETE_PENDING_LOCAL_VERIFICATION`.

Every finding was reproduced first on a clean worktree of `1c9d981` by `scripts/v2/m04_remediation_repro.py` (synthetic audit phrases only; app probes drive the real `AppDelegate` under the declared non-native shims of `tests/v2/lifecycle/`) → `acceptance/M04/remediation/repro_base_1c9d981.json`: **21/21 reproduced**. At `d31245f`: 0/21 (`repro_first_pass_d31245f.json`); at `4ef219c`: **0/21, no probe errors** (`repro_repaired_4ef219c.json`). The audit's exact outputs were source predictions; most matched, and where the runtime differed it is recorded below. No finding was refuted; 09 and 18 were narrowed.

### Dispositions

Common base for every reproduction: `1c9d981`. “Probe” = function in the repro script.

| Finding | Disposition | Probe → observed on base | Repair | Regression (fails on `1c9d981`) |
|---|---|---|---|---|
| 01 registered alias turns slash prose into a token (Critical) | reproduced | a01: “we should slash code review time” → `we should /code-review time` | first pass: modal/pronoun gate; review round (R1): a token is inserted only in a COMMAND POSITION — clause start or an explicit command frame (`slash_command_frames`, “to” only after a motion/change verb); elsewhere `no_command_frame` review record | `test_01_…`, review `test_R1_…` |
| 02 punctuation consumed / quantities merged | reproduced | a02: “twelve percent.” → `12%`; “twenty, five percent” → `25%`; “one hundred. five percent” → `105%` | token cores + structural barriers (edge punctuation, every Unicode line separator — R14); no grammar parses across a barrier; `crosses_delimiter` safety net; command words own their edge punctuation (R23) | `test_02_…`, `test_R14_…`, `test_R23_…` |
| 03 scaled decimals truncated | reproduced | a03: “one point two three four five thousand dollars” → `$1234` (ledger 1234.5000); “zero point zero zero zero one thousand dollars” → `$0` | exact scaled value (integral → int, else minimal exact fraction); `render_exact`, never `int()` | `test_03_…` (Decimal oracle incl. negatives and percent) |
| 04 zero/malformed/quantified scales | reproduced | a04: `$100`, `$1000`, `$1,001,000`, `$2,000,000`, `a few $100,000` | `scan_cardinal` tracks explicit zero vs omitted multiplier and scale order → `malformed_scale`; quantifier + bare scale → `quantified_scale`; both STRUCTURAL whole-span regions; review round: en “thousand and N” is one number (R2) | `test_04_…`, `test_R2_…` |
| 05 signs lost in typed values; negative port | reproduced | a05: `step -5` with value 5; `port -80` accepted (value 80); dimension `10x20 cm` for `10 × -20 cm` | `_signed()` composes sign everywhere; ports validated signed → `invalid_port` | `test_05_…` |
| 06 time steals count+duration | reproduced | a06: `3:15 minute breaks`, `1:20 minute session` | time needs a time preposition or a spoken meridiem; review round (R5): without a meridiem only clause material may follow (`time_followers`); juxtaposed number words stay words | `test_06_…`, `test_R5_…` |
| 07 modal may / invalid ordinal day | reproduced | a07: `this May 1 require approval`; `March 30 second` | ambiguous month words need evidence before AND date-shaped context after (R11); compound tens+ordinal day kept whole and flagged; narrow month-length/leap check (design D1); day-first invalid days flagged (R4) | `test_07_…`, `test_R4_…`, `test_R11_…` |
| 08 partial structured candidates | reproduced | a08: `one point 26.4`; `1 dot 2.3.4.5`; `point 5%`; `1 dot 2 dot three` | `grammar_numeric_chains`: unanchored version-speak, leading decimals (a digit after, not after a determiner — R12), dotted chains of non-IPv4 arity → STRUCTURAL regions that also block straddling proposals | `test_08_…`, `test_R12_…` |
| 09 punctuation nouns rewritten | reproduced, **narrowed** | a09: `. drama`, `time.`, `: cancer`, `\| tobacco`, `a?`, `the / character`, `-i asked him` | determiner guard for every name (articles/possessives only — R6), clause-start guard for trailing-punctuation/noun-ambiguous names with any following token (R7), pronoun “I” never a flag. **Narrowed:** “time period” and mid-clause “we discussed period drama/colon cancer/pipe tobacco” are surface-identical to accepted commands (“stop period”, “hello comma how…”); declared residuals (design D2) | `test_09_…`, `test_R6_…`, `test_R7_…` |
| 10 literal escape scope | reproduced | a10: 12-word payload's `period` → `.`; nested “write the word” dropped | payload = every token to the next delimiter, no cap, written tokens included (R8); nested markers are content; code fences/inline code protected (layer 2, adjudicated) | `test_10_…`, `test_R8_…` |
| 11 spoken paths lower-cased / hybrid | reproduced | a11: `/users/ada/myproject`; `/users/ada slash build2` | written spelling for path and email components; incomplete chains refused whole; dotted names inside components (R9) | `test_11_…`, `test_R9_…` |
| 12 mutable policy under a fixed revision | reproduced | a12: nested teens/symbols mutable, `12%`→`13%` with the same revision; cache poisoned fresh policies; caller dict mutated a ContextSnapshot | deep freeze of a private copy, sealed `LocaleTables` with `__slots__` (R19), immutable policy attributes, snapshotted context identifiers, cache never handed out | `test_12_…` (isolated interpreter), `test_R19_…` |
| 13 previous job's style becomes the next inherit | reproduced | a13 (real `_vocab_job_state`/`_finalized_policy`): configured technical → explicit standard → inherit gave standard (and the mirror) | configured base kept apart (`_norm_base_policy`); inherit resolves against it at hotkey-down, finalize, `_m10_finalize_upgrade`, preview (incl. its fallback — R18) and pipeline info | app `test_13_…`, `test_13b_…`, review `test_R18_…` |
| 14 equal-output same-span loses precedence | reproduced | a14: skill+identifier “slash period” → `slash.` | layer precedence decided before output agreement; same text with different typed values is ambiguous | `test_14_…` (incl. permutation) |
| 15 identifier no-op second-pass edit | reproduced | a15: second pass re-emits `user ID`→`user ID` | canonical spans emit nothing and claim the span | `test_15_…` |
| 16 evidence-write failures swallowed | reproduced | a16: each of 4 write/lease failures → no event, `not_captured_at_stage` | content-free `training.capture_failed` with the failed step; missing reason `retention_write_failed` (documented in the controlled vocabulary — R17); a fully published normalized text stays referenced and remains cleanup's parent (R16) | app `test_16_…`, review `test_R16_…` |
| 17 duration excludes apply | reproduced | a17: injected 50 ms apply delay reported as 0.35 ms | duration measured after application | `test_17_…` |
| 18 fixture oracles miss typed semantics (test gap) | reproduced, **narrowed** | a18: typed-sign mutation caught by one hand-picked test only; time rewrite and an unjustified idempotence flag not caught | independent typed oracle over every `protected_values` (65 numeric + 35 syntax; Decimal, ordered components, 24-h clock), ledger-sign == rendered-sign, fixed idempotence-exception inventory, semantic time negative | `test_18_…` (sign, unit, time, flag mutations) |
| 19 AC04 scanner incomplete (test gap) | reproduced | a19: nested helper with `import os as _o; getattr(_o, …)` not detected | recursive AST scan, fixed reviewed population, import allowlist incl. the `snippets` collaborator, dynamic-attribute check; runtime capability trap across snippet/file-tag composition | `test_19_…`, `test_19b_…` |
| 20 benchmark accepts a no-op (test gap) | reproduced | a20: identity normalizer → exit 0 | workload validity gate (exit 2) before speed; authored per-sentence outputs and summed edit counts (R21); no-match cohort gated; stress cohorts | `test_20_…`, `test_R21_…` |
| 21 caller/retry provenance (test gap) | reproduced | a21: a History retry after a standard-style job ran under standard (“twelve retries failed” unchanged) | reviewed caller inventory (one applied call on raw ASR; preview/diagnostic others); retries run under an unscoped `retry_unscoped_default` snapshot (R15), recorded as `policy_source` | app `test_21_…`, `test_21b_…`, `test_21c_…`, review `test_R15_…` |
| D1 calendar validity | adjudicated | — | narrow month-length + explicit-year leap check; nothing else inferred | `test_07_…` |
| D2 intent from surface | adjudicated | — | declared conservative ownership (command positions, structural guards); 8 surface-identical corpus cases recorded as residuals | corpus test |
| D3 edit replay vs regeneration | documented | — | ledger replay is independent of the parser; regeneration requires the retained policy/context identity — the revision identifies an immutable snapshot, `policy_source` names which snapshot | pipeline + corpus replay checks |

**Guardrails honored.** `number_policy="off"` was not treated as a style value; `$12 and 50 cents` is unchanged; qualifiers (“about 12%”) unchanged; no fractions, broader Spanish grammar, year parsing or filesystem lookup were added (es tables gained only the standard accented spellings of numbers they already covered).

### Independent review round

**Independence method.** The frozen first pass `d31245f` was reviewed by a separate subagent started with an empty context and only the audit, the contracts and the diff `1c9d981..d31245f`, in its own git worktree at `d31245f` (probes under a temporary directory; no repository write). It did not see the author's notes. Every finding it reported was re-run by the author on `d31245f` before any fix.

| ID | Defect left or introduced by the first pass | On `d31245f` | Fix in `4ef219c` |
|---|---|---|---|
| R1 | slash intent was a verb-word list: adverbs/nouns/“and” still produced tokens; command frames (“switch to”, “do”) lost | “we should really slash code review time” → `/code-review`; “switch to slash code review” literal | command-position ownership |
| R2 | “thousand and N” converted only the tail | “two thousand and twenty dollars” → `two thousand and $20` | en connector after a scale; tail guard (es) |
| R3 | prefix before a hyphenated compound converted | `300 sixty-five days` | compounds are number speech |
| R4 | tens before an ordinal converted; day-first invalid day unflagged | `the 20 fifth anniversary`; “the thirty second of May” unflagged | ordinal adjacency; day-first `invalid_day` |
| R5 | anchored times stole prices/measures | “sold at three fifty dollars” → `3:50 dollars` | clause-material follower rule |
| R6 | determiner guard killed commands after object pronouns | “let's do that period” literal | articles/possessives only |
| R7 | clause-start guard depended on a word following; second pass unstable | “Period 3 starts” → `. 3 starts`; “Period five” second pass changed | any following token |
| R8 | escape payload stopped at a written token | “write the phrase version 2 is twelve percent period” → `version 2 is 12%.` | payload to the delimiter |
| R9 | path/speech hybrids | `/etc/nginx dot conf` | dotted components |
| R10 | flag letters lower-cased; “dash I” positive lost | `ls -l` for “ls dash L” | written case; single-letter flag needs a command |
| R11 | “may” evidence fakeable | “we build on may first require updates” → `May 1` | date-shaped context after the date (day-first shape exempt — narrowed, see below) |
| R12 | noun “point”/“dot” blocked real numbers | “at this point twelve percent” literal | determiner-noun rule; fraction needs a digit |
| R13 | es accented spellings missing | `200 veintiséis por ciento` | accented table entries |
| R14 | only `\n`/`\r` were barriers | “twenty five percent” → `25%` | every Unicode line separator |
| R15 | a retry reused the previous job's workspace skills/context | “slash deploy prod” → `/deploy-prod` on the retry | `_retry_norm_state` unscoped snapshot |
| R16 | ledger failure left a published normalized text unreferenced; cleanup parent mislabeled raw | unreferenced text artifact | fully published text stays referenced |
| R17 | new missing reason outside the documented vocabulary | — | vocabulary documented (training_evidence.md, capabilities comment) |
| R18 | preview fallback used the last job's style | literal preview | configured profile in the fallback |
| R19 | `LocaleTables.__dict__` writable | `13%` with the same revision | `__slots__` |
| R20 | corpus component oracle used a prefix match | `10x-200 cm` accepted | exact component tuple + unit |
| R21 | benchmark validity gate loose | ~25 % of work silently skipped still passed | authored per-sentence oracle + summed edit counts |
| R22 | quadratic arbitration/application (pre-existing) | 16k edits 5.2 s | O(n log n) arbitration, single-pass join (16k edits ≈1.5 s) |
| R23 | stray comma after a command; “oh” over-blocked | “new line, hello” → `, hello`; “oh twelve people” literal | command tokens own their punctuation; “oh” only next to a digit |

Per-test results of `tests/v2/normalization/test_m04_review_round.py` (22 tests): `acceptance/M04/remediation/review_round_d31245f.json` **0/22 pass** and `review_round_4ef219c.json` **22/22 pass**. **Narrowed:** R11's pun “the fourth of may be with you” — the day-first shape “the Nth of <month>” is date evidence by itself (the fixture “the fourth of March works for me” must convert), so it still converts. **Pre-existing, not changed:** “(twelve percent)” and other paren/quote-attached words are not word tokens (tokenizer boundary, left for a later milestone); a clause-final bare number (“we saw twelve, then left”) stays words, as “the answer is twelve” always did.

### Adversarial corpus (development evidence)

`tests/v2/normalization/m04_audit_corpus.json` (sha256 `76139794d180f4e63c20f69f2901c0caf9460593ed1c80888e583230bf90e210`, verbatim) with per-case adjudications for all 52 intent/design cases (`m04_corpus_adjudications.json`), scored by `test_m04_audit_corpus.py` and `scripts/v2/m04_corpus_report.py` — oracles never call the grammar under test. It was consulted while repairing: it is development evidence, not an unseen holdout, and its variants share families.

| Code | pass | fail | declared residual | families with a failure |
|---|---|---|---|---|
| base `1c9d981` | 290 | 70 | 8 (also failing) | 43 of 171 |
| first pass `d31245f` | 360 | 0 | 8 | 0 |
| final `4ef219c` | **360** | **0** | **8** | 0 (5 families carry a residual) |

By role at `4ef219c`: adversarial probes 44/44, design probes 6/6, positive controls 50/50, profile relations 18/18, negative controls 2/2, false-positive controls 240/248 + 8 residual. The 8 residuals (design D2): “time period” ×4 (≡ “stop period”), “we discussed colon cancer / pipe tobacco / period drama” (≡ “hello comma how…”), and registered “slash costs” at utterance start (≡ “slash brainstorm”). The 10 stateful probes are covered by the repro script and the app/review suites (S001→a12/test_12, S002/S003→test_13, S004→test_14, S005→test_15, S006→test_16/R16, S007→test_18, S008→test_19, S009→test_20/R21, S010→test_21/R15).

### Contracts changed (and why)

`contracts/normalization.md`: structural delimiters and core spans, code zones, structural review regions and same-span precedence, time evidence and followers, ambiguous months and the narrow calendar check, number-speech adjacency, exact scaled decimals, signed ports, command-position slash tokens, symbol noun guards, flag case, literal-escape scope, path/email spelling, deep immutability, evidence changes; the historical 4.77 ms reference-Mac figure is marked historical. `contracts/training_evidence.md`: `retention_write_failed` in the controlled vocabulary, `policy_source` (`job_snapshot` | `retry_unscoped_default` | `current_default`). The policy file gained keys (so `policy_revision` changes, as it must for a semantic change). Envelope changes are additive.

### Portable verification (tested SHA `4ef219c`)

Interpreter: CPython 3.13.12 + numpy 2.5.3, SQLite 3.45.1 (scratch venv linked as `.venv`; the project venv is Python 3.14 on the Mac). Full per-file commands, exits and counts: `acceptance/M04/remediation/portable_tests_4ef219c.json`.

| Group | Files | ok lines | Failed |
|---|---|---|---|
| Required M04 suites (numbers 13, syntax 13, short commands 3) | 3 | 29 | 0 |
| M04 pipeline under the declared shim (7) | 1 | 7 | 0 |
| New M04 suites (corpus 2 = shape + 368-case oracle run, remediation 19, app seams 6, review round 22) | 4 | 49 | 0 |
| M01 privacy/inventory (baseline manifest 5, snapshot safety 19, registry 5) | 3 | 29 | 0 |
| M02 training evidence (collector 11, M02 remediation collector 14, app wiring 4) | 3 | 29 | 0 |
| M03 lifecycle around retry (M03 remediation app 14, M03 review round 15, lifecycle 13 under shim) | 3 | 42 | 0 |
| M05 registry/preview (matching 12, store 16, hint selection 9) | 3 | 37 | 0 |
| M06 context (snapshot 27, pipeline 12 under shim) | 2 | 39 | 0 |
| M07 normalized input/protection (cleanup engine 18, cleanup remediation 36) | 2 | 54 | 0 |
| M10 profiles/snippets/skills (snippets 9, style resolution 9, skills 6, developer resolution 6) | 4 | 30 | 0 |
| M14 classification/evidence (classification 7, dataset export 6, sampling 3, splits 4, learning 11, profile 9) | 6 | 40 | 0 |
| Model-backed (`training/test_live_pipeline.py`, `lifecycle/test_worker_live.py`, mlx suites) | — | — | **not run** (no MLX/models in the cloud) |

**Whole tree and downstream.** Every `tests/**/test_*.py` (78 pre-existing files) was run under the shim runner on `1c9d981` and on the repaired tree: each has the same exit status on both; the four new M04 suites pass. The ten AppKit-importing suites that fail partly under the shim on the base as well (the M03 addendum's list) were compared test by test in separate processes: all 95 test functions have byte-identical outcomes (54 pass / 41 shim-limited) on both — shim limits, not M04 regressions. One flake was seen once and not reproduced: `test_m03_remediation_worker.py` failed while two sweeps ran concurrently on the same CPU and passes alone and in the final sweep. Intended test edits outside the new suites: the numbers/syntax suites gained the typed oracle, the exception inventory and the AST scanner; `tests/v2/fixtures/manifest.json` declares the committed corpus (`m04_remediation_created`, not part of the 320 planned owning fixtures).

**Benchmark.** `scripts/v2/benchmark_m04.py` (repaired: workload-validity gate before speed — authored per-sentence outputs and summed edit counts, no-match cohort gated, stress cohorts, exit 2 invalid / 1 budget / 0 pass). Cloud run at `4ef219c` (`acceptance/M04/remediation/benchmark_cloud_4ef219c.json`, command `.venv/bin/python scripts/v2/benchmark_m04.py --out <dir>`): verdict **pass**; long_500 (502 words, 80 edits) p50 11.7 / p95 13.2 / p99 15.1 ms; long_500_plain (504 words, 0 edits) p95 9.9 ms; short p95 0.72 ms; medium p95 3.25 ms; dense_edits (498 words, 332 edits) p95 23.8 ms; command_words (425 edits) p95 22.1 ms; number_words 500 p95 8.8 ms; 4000 number words p95 70.5 ms (linear). Same container, the base with its original harness: long_500 p95 10.3 ms, plain 7.2 ms — the repair costs ≈28 % and stays well inside the 25 ms budget. Cloud numbers are algorithmic evidence only. The September 22 Mac run (`benchmarks/20260922-012259-m04/`) stays as recorded and is **stale** for the current code (M04-V003).

### Historical records (2026-09-22)

The September 22 M04 results, benchmark and the pending human item `native-m04-dictation-profiles` are preserved unchanged in `results.json` and above. Superseded for the current code: the 140-fixture pass as a semantic claim (the audit found its oracles insufficient; the fixtures still pass, now with the typed oracle), the benchmark figures (older code, weaker harness), and the documented time rule “start an utterance” (now: time preposition or meridiem). The pending human item is carried, with current instructions, by M04-V002.

### Pending local verification (`docs/v2/VERIFICATION.html` → M04, instructions M04-r1, code `4ef219c`)

Front-end edit made after invoking `anthropic-skills:frontend-design`; only the M04 placeholder and the “Last updated” line changed (everything else byte-identical). **M04-V001** Mac parity of the M04 suites under the project Python (real PyObjC for the app-seam and pipeline suites) · **M04-V002** native dictation under technical and standard (version, percent, IPv4, slash prose, modal “may”, bare integer) with the ledger read and replayed from the sandbox store — carries `native-m04-dictation-profiles` · **M04-V003** reference-Mac benchmark (new run; history untouched). No separate model-backed item: the ASR → normalization → cleanup interface did not change (V002 runs through the real models). All start **Not run**; M01–M03 IDs, revisions and results are untouched. Pending entries in VERIFICATION.html do not block starting the next read-only milestone audit during this campaign unless the remediation discovers that an unverified condition makes downstream static analysis invalid.

### Limits

`PENDING_LOCAL_VERIFICATION`: AppKit/PyObjC, microphone capture, real Parakeet ASR punctuation/casing (which the boundary, flag-case and month-evidence rules now read), real Qwen cleanup, the dev app and reference-Mac timing. The 8 corpus residuals and the command-frame allowlist are declared design choices (D2), not solved intent inference. Paren/quote-attached words are still not word tokens. The collector's half-published artifacts are unreferenced and follow the job's retention (no new store API was added).

### Merge readiness

Code: ready to merge into `main` from the cloud's point of view — every confirmed cloud-repairable finding is repaired, the independent review's confirmed findings are fixed with regressions that fail on the first pass, and no portable or targeted downstream regression was found. Native certification: not given — M04-V002 (real dictation, where ASR punctuation and casing meet the new rules) is the most informative first check.
