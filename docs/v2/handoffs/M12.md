# M12 Handoff — Scratchpad and local writing versions

**Milestone:** M12 · **Requirements:** LF-R19, LF-R28, LF-R30 · **State:** `implemented_pending_human`
**Base commit:** `96d387f` (branch `main`, remote `scalinity/LocalFlow`) — the M11 close-out ORCHESTRATION realignment; the M12 commit follows this handoff.
**Tree at start:** clean; no other worktrees; no other session owned any file M12 touched.
**Tree at end:** M12 adds `localflow/v2/notes.py`, `localflow/v2/note_export.py`, `localflow/v2/ui/scratchpad.py`, `tests/v2/notes/` (5 suites, 30 checks), `scripts/v2/benchmark_m12.py`, `docs/v2/{contracts/scratchpad.md, acceptance/M12/, benchmarks/20260923-033704-m12/}`; modifies `localflow/app.py` (the note seams: the PTT note binding, the delivery branch, the note commands, the quick-open item), `localflow/v2/{store,training}.py`, `localflow/v2/ui/{state,hub,transforms_panel}.py`, `tests/v2/lifecycle/test_lifecycle.py` (the insertion stub gains `busy`), `tests/v2/ui/test_hub_shell.py` (9-view indices) and `docs/v2/{STATUS.json, contracts/{INDEX,store,hub,training_evidence,transforms}.md}`. The capture/normalization/vocabulary/cleanup pipeline, the M08 insertion queue semantics, the worker protocol, the installed app and all M01–M11 artifacts are otherwise untouched (no build authorized).

## What shipped

1. **The note workspace at schema v8 (S20/S08).** `NoteStore` over `Store.submit`: immutable parent-linked revisions with `origin` (created/typed/dictated/transform/snippet/restore/attachment) and `trigger` (system/autosave/explicit — an autosave-debounce snapshot never claims to be an explicit one); the unsaved-tail-risk marker (AC01); managed image attachments under `v2-notes/` (0700/0600, insert-failure unlink, deletion purge); pinning; escaped local search; bounded version lists. Migration v8 is additive/idempotent, joins the torn-write repair set, and the live store migrated 7→8 with a pre-migration backup (verify clean, all rows intact).
2. **The Scratchpad Hub view (VIEWS 8→9, the M09 triple).** Notes list + search, tab strip, the `ScratchpadEditor` NSTextView bound to a pure-Python `NotesEditorModel` (serialized flushes — concurrent autosave can never interleave revisions; debounced autosaves off the UI thread; note switches flush the outgoing buffer), versions popup + Restore (copy-forward, editor rebinds so the next keystroke builds on the restore — AC02), the transform picker, Snapshot, Add Image…, exports, Pin, Delete.
3. **Dictation into the note — the internal destination.** A PTT start with the editor as the key window's first responder binds the job (note id + insertion point captured at hotkey-down, the M08 promise discipline). `_finishWithText_` delivers at that anchor as an immediate `dictated` revision with `source_job_id`; the job records `insertion_confirmed/scratchpad_note`. **The M08 external queue is never involved** — a closed/changed note routes to `saved_not_inserted/note_closed_during_dictation` with the clipboard offer, never a paste elsewhere (the M12 regression requirement, pinned both ways).
4. **Note-scope transforms + Save-to-Scratchpad (S16/S20).** The picker's definition over the selection, or the WHOLE NOTE — the coordinator coerces a range-less note capture to `(0, len)` so accept REPLACES the note; accept revalidates the captured source (`note_range_changed` → copy offer). Revisions record the M11 task identity; candidates carry `task_kind=transform_note`. The preview panel's honestly-disabled Save-to-Scratchpad is live (the output becomes a new note with task identity).
5. **History copy/move + quick-open (S20 task 4).** `hubSaveHistoryRow` reads the retained final text (applied → cleaned → raw); V2 jobs attribute `source_job_id`, legacy rows are honest `typed`; move applies delete-everywhere to the job and a legacy move degrades honestly to copy (legacy history is lossless by contract). Quick-open is one status-menu action (Hub + Scratchpad + fresh note) behind the same focus-steal guard as Open Hub, with the deferred intent carried to the settle flush.
6. **Export + evidence.** Markdown/plain through the M07 `document_nodes` renderer (AC03: supported structure preserved, unsupported elements reported and never silently dropped; a failed export cleans its partial files and retains the note). The S29.8 note family: content-free `notes` observations appended to existing examples' envelopes behind consent, `note_evidence_links` closed on deletion, repeated revisions never mint examples or duplicate counts, typed additions never labeled dictated (AC05; AC04 pinned with sockets disabled).

## Contracts and decisions

- New `contracts/scratchpad.md`; `INDEX.md` row + decision note; `store.md` v8 section (+ the fail-fast closed-store note); `hub.md` (9-view list, `notes_service`, the M12 commands); `training_evidence.md` M12 live status; `transforms.md` (Save-to-Scratchpad live, note-scope capture). No frozen identifier changed; registry stays 33/22; no ADRs — no spec deviation accepted.
- **Note eligibility for analytics/profile (the M13/M14 boundary, recorded here per the milestone instruction):** note revisions are local observations, never acoustic truth. A dictation counts ONCE — its job (and any M13 `usage_facts`) is the counter; note text never feeds word counts. Repeated saved versions of the same dictated content are revisions of ONE logical dictation joined through `note_revisions.source_job_id` + `note_evidence_links` — never new dictations. Typed/snippet content carries no `source_job_id` and no attributed span; it can never become an ASR example. Changed intent (restores, rewrites) is recorded as an event and stays M14's classification.
- **The internal destination is not an insertion transaction:** a note-bound dictation records no `insertions` row (no clipboard/AX transaction exists — the write is our own editor); History's lineage still resolves because the job's stage artifacts are retained exactly as on the external path.

## Verification (commands, results, kinds)

Full detail in `docs/v2/acceptance/M12/results.json`. All cwd = repo root, all exit 0:

- **Pre-change baselines:** 40 fast suites + 6 model-backed suites green (incl. the V1 control 51/51, 50.6 s).
- **Post-review (final code):** note store **7/7**, editor **4/4**, export **4/4**, evidence **5/5**, scratchpad hub **10/10** through the real coordinator; all 39 earlier fast suites; all six model-backed checks; the V1 control 51/51 (49.9 s); registry 33/22; privacy scan clean.
- **Benchmark** (`20260923-033704-m12/m12.json`): warm 10,000-word note open p95 **0.094 s** (budget 0.2); editor acknowledgment during concurrent save+search max **1.2 ms** (budget 50 ms); autosave flush p95 0.34 ms; 200-note search p95 0.18 ms.
- **Live store migrated 7 → 8** with a pre-migration backup; verify() clean; 10 jobs / 7 legacy rows / 5 transform definitions intact.

## /review-2 → /address record

Two parallel read-only reviewers (bug-hunting debugger; architecture/quality/security/data-integrity auditor). Verdict before addressing: **Request Changes** — 4 criticals after dedup, 13 warnings, ~14 suggestions.

Criticals (all in the AppKit binding layer the headless suites legitimately could not reach): (1) **both reviewers** — the Transform… button was dead (`NSRange.location/.length` are integer attributes, not methods — calling them raises TypeError) AND the hub called `tfRunNoteTransform` with the wrong arity and no definition picker at all; (2) **both reviewers** — a whole-note transform accept INSERTED at the caret instead of replacing the note; (3) **both reviewers** — Restore never rebound the editor, so the next keystroke silently reverted it.

`/address` fixed all four with regression tests driven through the production paths (the picker + button action end-to-end; `(0, len)` whole-note ranges coerced at the coordinator as defense in depth; the refresh rebinds whenever the persisted revision moves under a clean editor, with restore flushing the buffer first). Every warning fixed: select/reload share one loader generation (the just-created note appears in the list); tab close rebinds/clears the editor and note switches flush the outgoing debounce tail; a no-change flush clears the tail-risk marker (no false AC01 alarms); `scratchpad_receive` enforces the PTT-time note binding and delivers at the captured anchor; the History text fallback chain (applied → cleaned → raw — saved_not_inserted rows are rescuable); the `revision_count` column misalignment; the versions list bounded at 200 with an honest truncated flag. Suggestions applied: the shared `_conn_append_revision`/`_conn_latest` helpers replace the duplicated envelope SQL; stale evidence links close as `example_unavailable`; closed stores fail fast instead of hanging 15 s; `notes_meta`/`editor_origin` (dead schema/state) cut; attachment insert-failure unlink; marker alt-text sanitizing; slugged export names; export flushes the debounce tail; partial-export cleanup; pending attribution restored on a failed flush; `transform_note` task kind; `objc.python_method` decorators. **Deferred with reasons** (recorded in results.json): mid-word span placement stays a documented heuristic; two attributed arrivals inside one debounce window merge under the second's origin (sub-second window, both texts persist). **No findings rejected as incorrect.** During the loop, a 15-second suite hang was root-caused to the TEST HARNESS (a deferred Hub refresh executing against a closed harness's dead store writer — impossible in production) and closed with the harness-side `discard_pending` plus the store's fail-fast guard. Post-address, every suite, all six model-backed checks and the benchmark re-ran green on the fixed code. (The /address per-fix-commit cadence was adapted to this repo's milestone protocol: one close-out commit, no push — per the kickoff's constraints.)

## Human verification (pending; steps in results.json)

`native-m12-scratchpad-trial`: from Terminal.app — quick-open, dictate into the editor (attribution visible in History), typed list/code + autosave + Snapshot, an image attachment, a selection transform AND a whole-note transform (accept replaces), quit mid-burst and reopen to the tail-risk banner, restore an earlier version, History copy + move, Markdown/plain export inspection, screenshots at normal and enlarged text scale. The thirteen earlier pending trials stand in their own results files; none gates M13's automatable work; **14 human checks are open overall**.

## Limitations / unresolved

- Documented in `contracts/scratchpad.md`: revision growth (every autosave stores full content; the versions LIST is bounded at 200 but nothing coalesces — coalescing/retention is M13+/M16 work, the append-only contract forbids silent rewrites here); the sub-second two-arrivals window; the mid-word span heuristic; no global quick-open hotkey (menu action + its key equivalent; a second global grab needs hotkey-module work).
- The installed /Applications app still runs pre-M02 code (no build authorized); M12 exists in dev runs and tests.
- Unknown from M01–M11, unchanged: producer source of stats/dictionary/transforms; `instance.lock` producer; the M01 release-to-insert pilot (M15 gate).

## Privacy

Nothing private entered Git: tests are synthetic in-code (docs.example.com URLs, fake PNG bytes); the envelope's `notes` block and every `notes.*` event carry ids/origins/counts/reason codes only (note ids ride `detail` — the envelope's fixed field vocabulary); note text lives exclusively in the store's `note_revisions` under Application Support; attachments live under `v2-notes/` (0700/0600), never in the repo; the benchmark JSON carries latencies/counts over synthetic fixtures; the live v8 migration is additive with a backup under `v2-evidence/backups/`; the baseline-manifest privacy scan passes over every new path.

## Reusable patterns for M13+

- **The internal destination pattern:** a PTT-time binding (identity + anchor captured at hotkey-down, enforced at delivery, mismatch → the saved-not-inserted fallback) is how any future internal surface (Insights drafts, M14 review panes) should receive dictations — the external queue stays external-only.
- **Rebind-when-moved:** a view bound to a store row should rebind whenever the persisted row moved under a CLEAN local buffer — dirty wins, and the tail flush lands after; this closed the whole restore/tab-switch/stale-editor class in one guard.
- **One loader generation per select:** list+detail loads sharing a generation cannot supersede each other — the just-created row actually appears; the M10/M11 views' back-to-back reload+select would benefit if they ever show the same staleness.
- **Fail fast on dead writers:** a closed single-writer store should reject submits instantly; a 15-second hang masked as a "wedge" costs an hour of debugging that a millisecond error message ends.

## M13 eligibility

**Dependency-eligible: yes.** M13 (Accurate local analytics and usage insights) depends on M02, M09, M10, M11, M12 — all delivered. The note-eligibility boundary M13 must respect is recorded above and in `contracts/scratchpad.md`: dictations count once (jobs/usage_facts, never note text); note revisions join through `source_job_id`/`note_evidence_links`; typed content is never ASR data. Pending human checks (M12's trial and the earlier ones) do not gate M13's automatable work.
