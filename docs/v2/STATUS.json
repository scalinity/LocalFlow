{
  "schema_version": 1,
  "active_milestone": "M13",
  "status": "implemented_pending_human",
  "status_reason": "All automatable M13 work and checks passed on post-review code: usage analytics at store schema v9 (one usage_facts row per logical dictation — a retry reaching a terminal outcome REPLACES it and recomputes or deletes the departed day's aggregate; explicit transforms and re-pastes are separate activity kinds that never increment dictated words; the dictation-path auto-apply transform rides the dictation's own row; facts carry their own app/duration/word/mode/latency copies so usage survives content retention; unknown instants refused; DST-correct day buckets in the config-selected reporting zone with launch-time re-bucketing under versioned recomputation), terminal-path fact writes through the real coordinator on every branch (confirmed/posted-unverified/saved with parent-monotonic end-to-end, cancelled-while-holding, failed, the Scratchpad internal destination as ONE dictation, empty transcription as an honest zero; retry jobs carry their original capture instant/zone/app read from the store rows; every write guarded so analytics faults never disturb dictation), versioned daily aggregates always recomputed from facts inside the same writer op, the Insights Hub view (VIEWS 9→10 through the M09 triple: range/app/mode cohort filters, weighted WPM with its denominator over the same text-producing cohort, outcome/fallback/hit counts, sample-aware stage-vs-end-to-end latency percentiles with n, per-app/per-mode breakdowns, the dated table, the Undated count, the imported-legacy reconciliation line and the AC04 Definitions footer), retention and delete controls (the new usage knob's own expiry pass, prune_metadata now enforcing the M02 metadata knob with live-example/content pins and facts surviving, Settings' confirmed delete-all-usage, History's per-job delete-usage with legacy refusing), and the E19.4 readiness aggregates (five distinct outcome classes as counts-never-rates, honest join coverage that a dangling audio id lowers, per-view task eligibility, explicit not-available for M14/M15 concerns and population WER). The seven imported legacy rows reconcile unchanged (280/277 words, 350.3 s, fixed 11 — the unknown legacy wpm formula stays labeled legacy). Suites: usage store 11/11 (incl. the cross-day retry regression and a fact/aggregate invariant), insights service 6/6, insights hub 7/7 through the real coordinator, training data 13/13, hub shell 15/15 over the 10-view Hub, the three updated M12 note suites, plus the full 54-suite set with all six model-backed checks and the V1 control (51/51) re-ran green post-review. Benchmark: 50k synthetic facts — every Insights query p95 ≤ 58.5 ms warm (budget 200); the fact write on the busiest day p95 1.04 ms (aggregation runs inside the fact write — sub-millisecond terminal-path impact). Live store migrated 8→9 with a pre-migration backup; verify clean; legacy summary reconciles the audited figures exactly. The /review-2 → /address loop completed: 2 corroborated criticals (cross-day retry aggregate desync; retry provenance loss and a fabricated-instant fallback) and every warning fixed with regression tests (WPM one-cohort denominator, honest join coverage, live-example completeness denominator, null nearing-expiry, contract event drift, date-fragile fixtures, the readiness suite gap); suggestions applied (upsert provenance, count alignment, algorithm-version drift check, second timestamp format, in-op zone assignment, the boundary documentation). Pending is the native Insights trial — dictate across two apps and check the graphs against a hand count, retry without doubling, delete-usage controls, retention independence — it does not gate M14's automatable work; 15 human checks are open overall.",
  "source_commit": "d291a4bd7dbcbfbef835886b26e69a60e6927d4a",
  "source_commit_note": "M13 was implemented on top of this commit (the M12 close-out ORCHESTRATION.html realignment); the M13 commit follows.",
  "requirements": ["LF-R20", "LF-R28", "LF-R30"],
  "registry": "docs/v2/registry.json",
  "automated_results": ["docs/v2/acceptance/M13/results.json"],
  "benchmark_runs": ["docs/v2/benchmarks/20260924-005000-m13/m13.json"],
  "pending_human": [
    "native-m13-insights-trial"
  ],
  "handoff": "docs/v2/handoffs/M13.md",
  "next_milestone": "M14",
  "next_milestone_dependency_eligible": true
}
